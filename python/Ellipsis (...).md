Оператор `...` (Ellipsis) — это **встроенный объект-заглушка**, имеющий тип `ellipsis`. Он не является синтаксическим сахаром, а представляет собой **реальный синглтон-объект**, как `None`.

Это **легковесный объект**, используемый как **семантическая метка** «что-то пропущено».

### Где и как используется

#### 1. **Аннотации типов** — основное применение

Указывает на **переменное число элементов** в неизменяемых коллекциях:
```python
from typing import Tuple

# Хорошо: неизменяемый, хешируемый, чёткий контракт
def fetch_users_by_ids(user_ids: Tuple[int, ...]) -> list[UserDTO]:
    if not user_ids:
        raise ValueError("user_ids must not be empty")
    # Запрос в БД: SELECT * FROM users WHERE id = ANY(%s)
    return db.fetch_many("users", user_ids)

# Вызов
users = fetch_users_by_ids((1, 5, 12))  # Передаём tuple, а не list
```
**Почему так лучше:**

- Гарантирует **неизменяемость входных данных** (никто не изменит `user_ids` внутри функции)
- Явно говорит: «ожидается последовательность, но порядок важен, дубликаты допустимы»
- Поддерживается **mypy**, OpenAPI (через Pydantic), IDE

> ⚠️ Не делай так:  
> `def fetch(..., user_ids: list[int])` — позволяет передать мутабельный `list`, который могут случайно изменить.
#### 2. **Заглушка в коде** (аналог `pass`, но читаемее)
```python
class AbstractService:
    def process(self, data):
        ...  # будет реализовано в наследниках

def todo_feature():
    ...  # временная заглушка
```

### Best Practices

| Сценарий                  | Использование     |
| ------------------------- | ----------------- |
| Типизация кортежей        | `tuple[int, ...]` |
| Заглушка метода           | `def foo(): ...`  |
| Многомерные срезы (NumPy) | `arr[...]`        |
| Хранение в коллекции      | **избегать**      |
- В **Clean Architecture** часто используют `Tuple[DomainEntity, ...]` для строгой типизации коллекций
- Заглушки `...` делают **абстрактные базовые классы** читаемее
- `Callable[..., ReturnType]` — стандарт для DI-контейнеров и декораторов

> 🔒 Помните: `...` — это **не синтаксис**, а **объект**. Его можно передавать в функции, хранить в переменных, сравнивать через `is`.

### ✅ Почему `...` предпочтительнее

| Критерий                   | `pass`                     | `...`                          |
| -------------------------- | -------------------------- | ------------------------------ |
| **Семантика**              | «здесь ничего не делается» | «здесь будет реализация»       |
| **Читаемость**             | нейтральная                | явно указывает на **заглушку** |
| **Современный стиль**      | традиционный               | принят в typing, ABC, Pydantic |
| **Использование в stdlib** | редко в заглушках          | часто (например, в `typing`)   |
