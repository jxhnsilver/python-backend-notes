## Что такое срез (`slice`)?

Это **операция получения подпоследовательности** из списка, строки, кортежа и других упорядоченных коллекций.

Вместо ручного копирования через цикл — **одна запись вида `arr[start:stop:step]`**, которая делает всё безопасно, быстро и читаемо.
## 🔸 Базовый синтаксис
```python
sequence[start:stop:step]
```
- `start` — индекс начала (**включительно**), по умолчанию `0`
- `stop` — индекс конца (**не включительно**), по умолчанию — длина коллекции
- `step` — шаг между элементами, по умолчанию `1`
- `sequence` — это последовательность, упорядоченный набор чего-либо
	- Любой параметр можно **опустить**: `[:]`, `[::2]`, `[:-1]`
Если `step` отрицательный — срез идёт **с конца к началу**.
## 🔸 Что возвращает?

- **Новый объект** того же типа:  
    `[1,2,3][1:] → [2,3]`, `"abc"[::-1] → "cba"`, `(1,2,3)[:2] → (1,2)`
- **Никогда не падает** при выходе за границы:  
    `[1,2][10:20] → []`, `"hi"[-100:2] → "hi"`

---

## 🔸 Полезные шаблоны в алгоритмах

| Задача                      | Срез                 |
| --------------------------- | -------------------- |
| Скопировать список          | `copy = original[:]` |
| Разворот                    | `arr[::-1]`          |
| Чётные позиции              | `arr[::2]`           |
| Нечётные позиции            | `arr[1::2]`          |
| Первые `k` элементов        | `arr[:k]`            |
| Последние `k` элементов     | `arr[-k:]`           |
| Без последних `k` элементов | `arr[:-k]`           |
| Без последнего              | `arr[:-1]`           |
| Без первого                 | `arr[1:]`            |
| Каждый второй с конца       | `arr[::-2]`          |
## 🔸 Примеры в реальных алгоритмах

### ✅ Палиндром
```python
def is_palindrome(s: str) -> bool:
    return s == s[::-1]
```
### ✅ Циклический сдвиг вправо на `k`
```python
def rotate(arr: list, k: int) -> list:
    k %= len(arr)
    return arr[-k:] + arr[:-k]
```
### ✅ Обработка блоками
```python
data = [1, 2, 3, 4, 5, 6]
block_size = 2
for i in range(0, len(data), block_size):
    block = data[i:i + block_size]  # → [1,2], [3,4], [5,6]
```

## ⚠️ Важно

- Срезы работают **только для последовательностей** (`list`, `str`, `tuple`, `bytes`).
- **Не работают** для `set`, `dict`, `deque`.
- Создаёт **полную копию** — осторожно с большими данными.
- `step = 0` → ошибка: `ValueError: slice step cannot be zero`.

---

> 💡 Срез — это **идиоматичный, безопасный и быстрый** способ манипуляции подпоследовательностями. Используй его вместо ручных циклов, где это возможно.