
`tuple` (кортеж) — это **неизменяемая упорядоченная коллекция**, также хранящая гетерогенные объекты. По функциональности похож на `list`, но **без возможности модификации после создания**.

### Основные характеристики `tuple`

|Свойство|Значение|
|---|---|
|**Изменяемость**|❌ Нет|
|**Упорядоченность**|✅ Да|
|**Дубликаты**|✅ Разрешены|
|**Хранение в памяти**|В **heap**, как массив указателей (`PyObject*`), но **фиксированного размера**|
|**Реализация**|На C (в CPython), как структура с предопределённой длиной|
### Основные операции
```python
t = (1, "a", [2, 3])  # можно даже мутабельные объекты

# Доступ
x = t[0]              # O(1)

# Срезы
t[1:]                 # → ("a", [2, 3]) — создаёт новый tuple, O(k)

# Длина
len(t)                # O(1)

# Поиск
t.index("a")          # O(n)
"a" in t              # O(n)

# Распаковка
a, b, c = t           # удобно для возврата нескольких значений
```
❗ Нельзя: `t[0] = 2`, `t.append()`, `t.pop()` — **AttributeError** или **TypeError**.

### Сложность операций

| Операция      | Сложность |
| ------------- | --------- |
| `t[i]`        | O(1)      |
| `x in t`      | O(n)      |
| `t.index(x)`  | O(n)      |
| Срез `t[a:b]` | O(k)      |
| Создание      | O(n)      |
| Сравнение     | O(n)      |
Нет операций модификации → **никаких O(n) из-за сдвига**, как в `list.insert(0)`.

### Как хранится в памяти

- Как и `list`, `tuple` — массив указателей на объекты в куче.
- **Отличие**: размер фиксирован при создании → **нет overhead на "резервное пространство"**.
- Более **памяти-эффективен**, чем `list` (меньше служебных полей).
- Может быть **интернирован** (например, маленькие кортежи кэшируются CPython).

### Когда использовать `tuple`

- Для **неизменяемых данных**: координаты `(x, y)`, RGB `(255, 0, 0)`
- В качестве **ключа в словаре** или элемента `set` (т.к. хешируем, если все элементы хешируемы)
- Для **возврата нескольких значений** из функции
- Для **гарантии целостности данных** (защита от случайного изменения)

### Почему это "лучше"

- **Безопасность**: неизменяемость → меньше багов, потокобезопасность.
- **Производительность**: меньше памяти, быстрее создаётся, хешируется (если возможно).
- **Семантика**: сигнализирует, что данные **логически связаны и не должны меняться**.
- **Соответствие SOLID**: особенно **принципу инверсии зависимостей** — можно передавать как неизменяемый контракт.

> 💡 Совет: используйте `tuple`, когда данные **логически образуют единое целое** и не требуют модификации.