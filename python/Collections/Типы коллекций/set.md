`set` — это **изменяемая неупорядоченная коллекция уникальных хешируемых объектов**, реализованная как **хеш-таблица без значений** (только ключи). Аналог математического множества.
### Основные характеристики `set`

| Свойство                    | Значение                                                                                            |
| --------------------------- | --------------------------------------------------------------------------------------------------- |
| **Изменяемость**            | ✅ Да                                                                                                |
| **Упорядоченность**         | ❌ Нет (до Python 3.6 порядок случайный; с 3.7+ — **реализация-зависимый**, но **не гарантируется**) |
| **Уникальность**            | ✅ Все элементы уникальны                                                                            |
| **Хешируемость элементов**  | ✅ Только хешируемые объекты (`int`, `str`, `tuple`, `frozenset`)                                    |
| **Хранение в памяти**       | В **heap**, как хеш-таблица, аналогичная `dict`, но **без значений** (только ключи)                 |
| **Математические операции** | ✅ Объединение, пересечение, разность и т.д.                                                         |
### Основные операции
```python
s = {1, 2, 3}

# Добавление
s.add(4)        # O(1) амортизированно

# Удаление
s.remove(2)     # O(1), KeyError если нет
s.discard(2)    # O(1), без ошибки

# Проверка наличия
3 in s          # O(1)

# Объединение
s1 = {1, 2}
s2 = {2, 3}
s1 | s2         # → {1, 2, 3}
s1.union(s2)    # то же

# Пересечение
s1 & s2         # → {2}

# Разность
s1 - s2         # → {1}

# Симметрическая разность
s1 ^ s2         # → {1, 3}

# Итерация
for x in s: ... # порядок не гарантирован
```

### Алгоритмическая сложность

| Операция          | Сложность                |
| ----------------- | ------------------------ |
| `x in s`          | O(1) амортизированно     |
| `s.add(x)`        | O(1)                     |
| `s.remove(x)`     | O(1)                     |
| `s.discard(x)`    | O(1)                     |
| `len(s)`          | O(1)                     |
| Объединение (`    | `)                       |
| Пересечение (`&`) | O(min(len(s1), len(s2))) |
⚠️ Худший случай — O(n) из-за коллизий, но на практике почти всегда O(1).

### ак хранится в памяти

- Внутренняя структура **аналогична `dict`**, но:
    - только ключи (значения не хранятся)
    - меньше overhead по памяти, чем `dict` с `None`-значениями
- При заполнении ~2/3 — таблица **увеличивается**, элементы **перехешируются**

### Когда использовать `set`
1. **Удаление дубликатов из списка**:
	```python
	unique = list(set(items))
	```
2. **Быстрая проверка принадлежности** (вместо `if x in list` → O(n)):
```python
valid_ids = {101, 102, 103}
if user.id in valid_ids: ...
```
3. **Математические операции над коллекциями**:
    - «Какие права есть у пользователя, но отсутствуют в роли?»
    - «Какие теги общие у двух статей?»
4. **Сбор уникальных значений "на лету"**:
```python
seen = set()
for item in stream:
    if item in seen: continue
    seen.add(item)
```
### ⚠️ Ограничения

- **Нельзя хранить мутабельные объекты**:
```python
s = {[1, 2]}  # TypeError: unhashable type: 'list'
```
- **Нет индексации**:
```python
s[0]  # TypeError — не упорядочен!
```
- **Порядок не гарантируется** → не используй для последовательных данных
### Read-only аналог: `frozenset`
```python
fs = frozenset([1, 2, 3])
# fs.add(4) → AttributeError
# Можно использовать как ключ в dict:
d = {fs: "allowed"}
```
### Пример из backend-разработки
```python
# Проверка, все ли обязательные поля формы заполнены
required_fields = {"email", "password", "name"}
submitted = set(request.json.keys())

if not required_fields <= submitted:
    missing = required_fields - submitted
    raise HTTPException(400, f"Missing fields: {missing}")
```

### Почему `set` — хороший выбор

- **Производительность**: O(1) для проверки принадлежности (в отличие от O(n) у `list`)
- **Семантика**: явно выражает «уникальный набор без порядка»
- **Безопасность**: автоматически убирает дубли
- **Соответствие SOLID**: инкапсулирует логику уникальности, легко тестируем

> 🔑 Используй `set` **везде, где не важен порядок, но важна уникальность и скорость поиска**.