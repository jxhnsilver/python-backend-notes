`dict` — это **изменяемая хеш-таблица**, реализующая **ассоциативный массив** (отображение _ключ → значение_). Это одна из самых частых и важных коллекций в backend-разработке.

### Основные характеристики `dict`

| Свойство                | Значение                                                                                   |
| ----------------------- | ------------------------------------------------------------------------------------------ |
| **Изменяемость**        | ✅ Да                                                                                       |
| **Упорядоченность**     | ✅ Да (с Python 3.7+ — порядок вставки гарантируется)                                       |
| **Ключи**               | Должны быть **хешируемыми** (`int`, `str`, `tuple`, `frozenset`, ...)                      |
| **Значения**            | Любые объекты (включая `None`, `list`, другие `dict`)                                      |
| **Уникальность ключей** | ✅ Ключи уникальны; дубликат → перезапись                                                   |
| **Хранение в памяти**   | В **heap**, как хеш-таблица с разрешением коллизий методом **open addressing** (в CPython) |
### Основные операции
```python
d = {"name": "Alice", "age": 30}

# Чтение
d["name"]          # → "Alice", O(1) амортизированно
d.get("city")      # → None (без исключения)

# Запись / обновление
d["age"] = 31      # O(1)
d.update({"city": "NYC"})  # объединение

# Удаление
del d["age"]       # O(1)
d.pop("name")      # → "Alice", O(1)

# Проверка наличия
"name" in d        # → bool, O(1)

# Итерация
for key in d: ...              # по ключам
for k, v in d.items(): ...     # по парам
```
### 1. `dict.get(key[, default])`
**Возвращает значение по ключу. Если ключа нет — возвращает `default` (по умолчанию `None`). Не вызывает исключение.**
```python
d = {"name": "Илья", "age": 25}
print(d.get("name"))        # → Илья
print(d.get("city", "Москва"))  # → Москва
```
### 2. `dict.keys()`
**Возвращает объект, содержащий все ключи словаря (view-объект). Можно превратить в список.**
```python
d = {"a": 1, "b": 2}
print(list(d.keys()))       # → ['a', 'b']
```
### 3. `dict.values()`
**Возвращает все значения словаря.**
```python
d = {"a": 1, "b": 2}
print(list(d.values()))     # → [1, 2]
```
### 4. `dict.items()`
**Возвращает пары `(ключ, значение)` в виде кортежей. Очень часто используется в циклах.**
```python
d = {"name": "Илья", "age": 25}
for k, v in d.items():
    print(f"{k}: {v}")
# Вывод:
# name: Илья
# age: 25
```
### 5. `dict.pop(key[, default])`
**Удаляет ключ и возвращает его значение. Если ключа нет — возвращает `default` или вызывает `KeyError`.**
```python
d = {"a": 1, "b": 2}
value = d.pop("a")
print(value)                # → 1
print(d)                    # → {'b': 2}
```
### 6. `dict.update(other)`
**Обновляет словарь парами из другого словаря (или итерируемого). Добавляет новые, перезаписывает существующие.**
```python
d = {"a": 1, "b": 2}
d.update({"b": 20, "c": 3})
print(d)                    # → {'a': 1, 'b': 20, 'c': 3}
```
### 7. `dict.setdefault(key, default)`
**Если ключа нет — добавляет его со значением `default` и возвращает `default`.  
Если есть — возвращает текущее значение (не меняет словарь).**
```python
d = {"a": 1}
d.setdefault("b", 0)
print(d)                    # → {'a': 1, 'b': 0}

d.setdefault("a", 99)       # "a" уже есть → не меняет
print(d["a"])               # → 1
```
### 8. `dict.clear()`
**Полностью очищает словарь (удаляет все элементы).**
```python
d = {"a": 1, "b": 2}
d.clear()
print(d)                    # → {}
```
### 💡 Какой метод использовать когда?

| Задача                            | Метод          |
| --------------------------------- | -------------- |
| Безопасно прочитать значение      | `get()`        |
| Пройтись по парам ключ-значение   | `items()`      |
| Добавить значение, если ключа нет | `setdefault()` |
| Удалить и получить значение       | `pop()`        |
| Объединить два словаря            | `update()`     |
### Алгоритмическая сложность

|Операция|Сложность|
|---|---|
|`d[key]`|O(1) амортизированно|
|`key in d`|O(1)|
|`d.get(key)`|O(1)|
|`d[key] = value`|O(1)|
|`del d[key]`|O(1)|
|`d.pop(key)`|O(1)|
|Итерация|O(n)|
|Создание|O(n)|
В худшем случае (много коллизий) — O(n), но в реальности почти всегда O(1).

### Как хранится в памяти (CPython)

- Внутри — **массив слотов** (buckets), каждый содержит:
    - хеш ключа
    - указатель на ключ
    - указатель на значение
- При коллизии — используется **open addressing** (линейный пробинг)
- При ~2/3 заполнения — таблица **увеличивается** (resize), все элементы **перехешируются**

> 💡 Резерв памяти позволяет сохранять O(1), но делает `dict` **менее памяти-эффективным**, чем `list` или `tuple`.

### Когда использовать `dict`

- Для **быстрого поиска по ключу** (вместо `list` + `index()`)
- Как **DTO / модель ответа API**
- Для **кэширования**
- Как **конфигурация** или **контекст выполнения**
### Почему `dict` — хороший выбор

- **Производительность**: O(1) для основных операций
- **Читаемость**: `user["email"]` понятнее `user[1]`
- **Гибкость**: поддержка вложенных структур, динамическое добавление
- **Соответствие SOLID**: легко инкапсулировать в репозиторий или mapper

