`dict` — это **изменяемая хеш-таблица**, реализующая **ассоциативный массив** (отображение _ключ → значение_). Это одна из самых частых и важных коллекций в backend-разработке.

### Основные характеристики `dict`

| Свойство                | Значение                                                                                   |
| ----------------------- | ------------------------------------------------------------------------------------------ |
| **Изменяемость**        | ✅ Да                                                                                       |
| **Упорядоченность**     | ✅ Да (с Python 3.7+ — порядок вставки гарантируется)                                       |
| **Ключи**               | Должны быть **хешируемыми** (`int`, `str`, `tuple`, `frozenset`, ...)                      |
| **Значения**            | Любые объекты (включая `None`, `list`, другие `dict`)                                      |
| **Уникальность ключей** | ✅ Ключи уникальны; дубликат → перезапись                                                   |
| **Хранение в памяти**   | В **heap**, как хеш-таблица с разрешением коллизий методом **open addressing** (в CPython) |
### Основные операции
```python
d = {"name": "Alice", "age": 30}

# Чтение
d["name"]          # → "Alice", O(1) амортизированно
d.get("city")      # → None (без исключения)

# Запись / обновление
d["age"] = 31      # O(1)
d.update({"city": "NYC"})  # объединение

# Удаление
del d["age"]       # O(1)
d.pop("name")      # → "Alice", O(1)

# Проверка наличия
"name" in d        # → bool, O(1)

# Итерация
for key in d: ...              # по ключам
for k, v in d.items(): ...     # по парам
```

### Алгоритмическая сложность

|Операция|Сложность|
|---|---|
|`d[key]`|O(1) амортизированно|
|`key in d`|O(1)|
|`d.get(key)`|O(1)|
|`d[key] = value`|O(1)|
|`del d[key]`|O(1)|
|`d.pop(key)`|O(1)|
|Итерация|O(n)|
|Создание|O(n)|
В худшем случае (много коллизий) — O(n), но в реальности почти всегда O(1).

### Как хранится в памяти (CPython)

- Внутри — **массив слотов** (buckets), каждый содержит:
    - хеш ключа
    - указатель на ключ
    - указатель на значение
- При коллизии — используется **open addressing** (линейный пробинг)
- При ~2/3 заполнения — таблица **увеличивается** (resize), все элементы **перехешируются**

> 💡 Резерв памяти позволяет сохранять O(1), но делает `dict` **менее памяти-эффективным**, чем `list` или `tuple`.

### Когда использовать `dict`

- Для **быстрого поиска по ключу** (вместо `list` + `index()`)
- Как **DTO / модель ответа API**
- Для **кэширования**
- Как **конфигурация** или **контекст выполнения**
### Почему `dict` — хороший выбор

- **Производительность**: O(1) для основных операций
- **Читаемость**: `user["email"]` понятнее `user[1]`
- **Гибкость**: поддержка вложенных структур, динамическое добавление
- **Соответствие SOLID**: легко инкапсулировать в репозиторий или mapper

