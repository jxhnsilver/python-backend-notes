### ✅ Основные теги комментариев

| Тег       | Значение                    | Когда использовать                                | Пример                                     |
| --------- | --------------------------- | ------------------------------------------------- | ------------------------------------------ |
| `# TODO`  | **Нужно доделать**          | Функциональность не реализована, но запланирована | `# TODO: add email validation`             |
| `# FIXME` | **Нужно исправить**         | Есть баг, костыль или временное решение           | `# FIXME: this breaks for empty input`     |
| `# NOTE`  | **Важное пояснение**        | Нет проблемы, но нужно пояснение для читателя     | `# NOTE: this must be called after init()` |
### Подробнее по каждому

#### 1. `# TODO`

- **Цель**: отметить **незавершённую работу**
- **Ответственный**: ты или команда — нужно вернуться и доделать
- **Важно**: не оставлять в `main` без причины
```python
def calculate_discount(user):
    # TODO: implement tiered discount logic
    return 0.1
```
#### 2. `# FIXME`

- **Цель**: указать на **существующую проблему**
- **Отличие от `TODO`**: здесь уже есть код, но он **работает неправильно**
```python
def parse_date(s):
    # FIXME: fails on ISO format '2023-12-01'
    return datetime.strptime(s, "%d/%m/%Y")
```
#### 3. `# NOTE`

- **Цель**: пояснить **нетривиальное решение**, которое не очевидно из кода
- **Не заменяй им документацию** — только для контекста
```python
def process_payment():
    # NOTE: retry 3 times because payment gateway is flaky
    for _ in range(3):
        if gateway.call(): break
```

**«Не оставляй `TODO`/`FIXME` в продакшене без трекинга — это техдолг»** означает следующее:
### 🔹 Что такое **продакшен (production)**

Это **боевая среда**, где работает реальный сервис, который используют клиенты (пациенты в «МЕДТОЧКЕ», пользователи API и т.д.).

---
### 🔹 Что такое **технический долг (technical debt)**

Это **компромисс в коде**, который:
- позволяет быстро выпустить фичу **сейчас**,
- но **требует доработки в будущем**, иначе:
    - растёт риск багов,
    - усложняется поддержка,
    - замедляется разработка новых фич.

`TODO` и `FIXME` — это **явные маркеры технического долга**.
### 🔸 Почему «без трекинга» — плохо?

Если вы просто написали:
```python
def send_notification(user):
    # TODO: add SMS fallback
    email.send(user.email)
```
— и **закоммитили это в `main`**, но **не завели задачу** в Jira/Trello/Notion:

|Проблема|Последствие|
|---|---|
|**Забудут про это**|Через месяц никто не вспомнит, что нужно доделать|
|**Нет ответственного**|Непонятно, кто должен это реализовать|
|**Нет приоритета**|Неясно, срочно это или нет|
|**Растёт долг незаметно**|В коде накапливаются десятки `TODO` — система становится хрупкой|
**Трекинг** = завести задачу в системе управления работой, привязать к релизу или спринту, назначить исполнителя.

### Как делать правильно (best practice)

1. **Написали `# TODO` в коде** → сразу создаёте задачу в трекере.
2. **Указываете ссылку на задачу в комментарии**:
```python
# TODO: add SMS fallback (MED-456)
```
3. **В задаче уточняете**:
    - что сделать,
    - зачем (бизнес-причина),
    - к какому релизу это критично.
4. **Перед мержем в `main`** — согласовываете с тимлидом, можно ли оставить `TODO` в продакшене.

> 🟢 Допустимо оставить `TODO` в продакшене **только если**:
> - это **временное решение** (например, заглушка под внешний API, который ещё не готов),
> - есть **чёткий план** по устранению (и задача в трекере!),
> - это **не влияет на безопасность/стабильность**.