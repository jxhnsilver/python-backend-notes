## Что значит «интерпретируемый»?

Исходный код **выполняется построчно** специальной программой — **интерпретатором** — без предварительной компиляции в машинный код.

> Но на самом деле: Python **компилируется в байт-код**, а затем **интерпретируется**.  
> Поэтому точнее: **«компилируемо-интерпретируемый»**.

## Этапы выполнения Python-кода

1. **Исходный код** (`hello.py`)  
2. → **Компиляция в байт-код** (внутренний язык, платформонезависимые инструкции) 
   - Результат: `.pyc` файл в `__pycache__/` (Байт-код сохраняется в папке `__pycache__` — чтобы в следующий раз запуск был быстрее.)
3. → **Интерпретация байт-кода** виртуальной машиной (CPython VM) (построчно выполняет байт-код)

> ⚠️ Байт-код **не равен машинному коду**. Он исполняется **внутри интерпретатора**, а не процессором напрямую.

**Что это значит?**

- **Машинный код** — это команды, которые понимает **процессор** напрямую (например, `mov eax, 5`).  
    Их генерируют компиляторы (C, Rust, Go).
- **Байт-код Python** — это **условные инструкции для виртуальной машины Python**, а не для процессора.  
    Например: `LOAD_CONST`, `BINARY_ADD`, `RETURN_VALUE`.
    
👉 Процессор **не может выполнить байт-код сам по себе**.  
Нужен **интерпретатор Python** (например, `python.exe`), который **читает байт-код и сам делает нужные действия на машинном уровне**.

## Преимущества интерпретируемого подхода

- ✅ **Кроссплатформенность**: байт-код одинаков на всех ОС (если версия Python та же).
- ✅ **Быстрая разработка**: запуск без этапа компиляции.
- ✅**Простое управление зависимостями и окружением**
	- Нет «сборки под платформу» → достаточно `pip install`.
	- Виртуальные окружения (`venv`) из коробки — изоляция без Docker (на этапе разработки).
	- `requirements.txt` или `pyproject.toml` — читаемый, человеко-ориентированный способ фиксации версий.

## Недостатки

- ❌ **Медленнее**, чем нативный код (из-за интерпретации).
	Потому что:
	- Интерпретатор **читает байт-код по инструкции**.
	- На каждую операцию тратится время на **диспетчеризацию** («что делать дальше?»).
	- Нет оптимизаций на уровне процессора (как в JIT или AOT-компиляторах).
	
	Но: для **I/O-операций** (БД, HTTP, файлы) — это почти не важно. Скорость зависит от сети/диска, а не Python.
- ❌ **Ошибки видны только в runtime** (нет compile-time проверки, Python ничего не скажет, пока не дойдёшь до этой строки при запуске).
- ❌ **GIL в CPython** — мешает реальному параллелизму в потоках (Global Interpreter Lock — это «замок», который **разрешает работать только одному потоку Python-кода за раз**.).
- ❌ **Исходный код легко извлекается** (байт-код декомпилируется).