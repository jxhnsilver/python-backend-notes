**Добавление миграций в существующее приложение — кратко и по делу**

---

### 📌 Ситуация

У вас есть приложение:

- с готовыми моделями (`models.py`),
- и таблицы в БД уже созданы **вручную** (через `syncdb` или SQL),
- но **миграций нет** (например, проект на старой версии Django).
### Что делать

1. **Создать initial-миграцию**:
```bash
python manage.py makemigrations your_app_label
```
→ Создаст `0001_initial.py`, описывающую текущие модели.
2. **«Привязать» миграцию к существующей БД**:
```bash
python manage.py migrate --fake-initial
```
→ Django проверит, что таблицы уже есть, и **запишет миграцию как применённую**, не выполняя SQL.

### ⚠️ Важные условия (иначе сломается)

1. **Модели не менялись** после создания таблиц.  
    — Если вы уже правили `models.py` **после** создания таблиц, то миграция будет описывать **новое состояние**, а не то, что есть в БД → `--fake-initial` **не сработает** или приведёт к рассинхрону.
2. **БД не правили вручную** (типы колонок, ограничения и т.д.).  
    — Django **не сверяет структуру БД с моделями**, только наличие таблиц/колонок по имени.  
    — Если в БД что-то не совпадает — следующая миграция упадёт с ошибкой.
### ❌ Что будет без `--fake-initial`
```bash
python manage.py migrate
```
→ Попытается **создать таблицы**, которые уже есть → ошибка вроде  
`relation "your_table" already exists`.
### ✅ Итог

- `makemigrations your_app` + `migrate --fake-initial` — **единственный правильный способ** подключить миграции к legacy-приложению **с неизменёнными моделями и БД**.
- После этого — работайте как обычно: меняйте модели → `makemigrations` → `migrate`.
