## Workflow миграций в Django
### 1. **Изменяете модели**

Например:

- Добавляете/удаляете поле
- Удаляете модель
- Меняете связи (`ForeignKey`, `ManyToMany` и т.д.)
### 2. **Генерируете миграцию**
```bash
python manage.py makemigrations
```
→ Django сканирует `models.py`, сравнивает с последними миграциями и создаёт новую.

> 🔍 **Всегда читайте вывод!**  
> `makemigrations` не всегда угадывает сложные изменения (например, переименование поля может быть распознано как «удалить + добавить»).

#### Добавить осмысленное имя:
```bash
python manage.py makemigrations --name add_published_field books
```
### 3. **Применяете миграцию к БД**
```bash
python manage.py migrate
```
→ Выполняются SQL-операции, изменения вносятся в БД.

> ✅ **Проверяйте**, что миграция прошла без ошибок.

### 4. **Коммитите изменения в Git**

Коммит должен включать **оба файла**:

- `models.py` (изменённые модели)
- `myapp/migrations/000X_*.py` (новая миграция)

> 🎯 Это гарантирует, что у других разработчиков и на проде структура БД будет соответствовать коду.

## Конфликты миграций

Если два разработчика создали миграции с одним номером (например, `0003_...` в одном приложении):

- Django **не использует номера** для порядка — важны **имена** и **зависимости** в `dependencies = [...]`.
- При обнаружении «развилки» (`divergence`) Django предложит:
    - автоматически объединить (`makemigrations --merge`)
    - или вручную указать зависимости.

> ✅ Это нормально и легко разрешается — не паникуйте.

