### Что такое файл миграции?

Это **обычный Python-файл** в папке `app/migrations/`, описывающий **изменения схемы базы данных** в декларативном виде (описание **желаемого результата**).
```python
from django.db import migrations, models

class Migration(migrations.Migration):
    dependencies = [("authors", "0001_initial")]
    operations = [
        migrations.DeleteModel("Tribble"),
        migrations.AddField("Author", "rating", models.IntegerField(default=0)),
    ]
```
### Что ищет Django при загрузке?

Класс с именем `Migration`, наследующий от `django.db.migrations.Migration`.  
Основные атрибуты:

| Атрибут        | Назначение                                                                        |
| -------------- | --------------------------------------------------------------------------------- |
| `dependencies` | Список миграций, от которых зависит текущая (формат: `[('app', '0001_initial')]`) |
| `operations`   | Список **декларативных операций**, описывающих изменения схемы                    |
### Как это работает?

1. Django **выполняет все миграции по порядку** в памяти — создаёт виртуальную модель состояния БД.
2. Сравнивает это состояние с текущим кодом в `models.py`.
3. На основе различий генерирует новые миграции (`makemigrations`).
4. При `migrate` преобразует `operations` в **реальные SQL-запросы** и применяет их к БД.
### Можно ли редактировать вручную?

- **Редко нужно**, но **можно и иногда нужно**:
    - автогенерация не сработала корректно (например, переименование поля),
    - требуется **data migration** (обновление данных, а не схемы),
    - нужны сложные операции, которые не детектируются автоматически (`RunPython`, `RunSQL`).

> 💡 Django **не запрещает** ручное редактирование — файлы полностью в вашем контроле.

### Главное

- Миграции — **декларативные**, а не императивные: вы описываете **что**, а не **как**.
- Порядок выполнения гарантируется через `dependencies`.
- Не бойтесь читать и при необходимости править файлы — это нормальная часть работы с БД в Django.
