**Миграции** — это способ изменять структуру базы данных вместе с кодом. Когда вы меняете модели в `models.py`, миграции превращают эти изменения в команды SQL для базы данных.

**Миграции — это система контроля версий для структуры базы данных**, которая превращает изменения в Python-моделях в команды для БД и отслеживает их выполнение.

Миграции — это **способы отслеживания и применения изменений структуры базы данных**, основанных на изменениях в ваших **моделях Django** (`models.py`).  
Django использует миграции, чтобы:

- автоматически генерировать SQL-команды (CREATE, ALTER и т.п.),
- безопасно применять их к вашей БД,
- отслеживать, какие изменения уже применены (через таблицу `django_migrations`).

Миграции — это **Python-файлы**, хранящиеся в папке `myapp/migrations/`.

**Зачем нужны?**  
Чтобы синхронизировать код (модели) и реальную БД без ручного написания SQL.

**Основные особенности:**

- 📄 Хранятся в `app/migrations/000X_name.py`
- ⏳ Выполняются **последовательно**, каждая зависит от предыдущей (`dependencies`)
- ✅ Django отслеживает применённые миграции через таблицу `django_migrations`
- 🔄 Поддерживают **откат** (если не используются необратимые операции)
- 🤝 Работают одинаково со всеми поддерживаемыми СУБД (PostgreSQL, SQLite, MySQL и др.)
- 🔁 Для каждой миграции можно посмотреть SQL: `python manage.py sqlmigrate app 000X`
- 📦 **Коммитятся в Git** — это часть кодовой базы
