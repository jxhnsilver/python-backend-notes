### Что такое data migration?

Это миграция, которая **изменяет данные в БД**, а не структуру.  
Примеры:

- Заполнить новое поле на основе старых,
- Очистить устаревшие записи,
- Перенести данные между моделями.
### Как создать

1. Сгенерируй **пустую миграцию**:
```bash
python manage.py makemigrations --empty yourapp
```
2. Добавь в неё `RunPython` с функцией обработки:
```python
from django.db import migrations

def combine_names(apps, schema_editor):
    Person = apps.get_model("yourapp", "Person")  # historical model
    for person in Person.objects.all():
        person.name = f"{person.first_name} {person.last_name}"
        person.save()

def reverse_combine_names(apps, schema_editor):
    Person = apps.get_model("yourapp", "Person")
    Person.objects.update(name="")

class Migration(migrations.Migration):
    dependencies = [("yourapp", "0001_initial")]
    operations = [
        migrations.RunPython(combine_names, reverse_combine_names),
    ]
```
### ⚠️ Главные правила

- **Используй `apps.get_model()`** — никогда не импортируй модели напрямую.
- **Не полагайся на кастомные методы** (`save()`, `__init__()` и т.д.) — они **не работают** в миграциях.
- **Всегда пиши `reverse_code`**, если откат возможен. Без него — миграция необратима.

---

### 📌 Когда использовать

- После добавления нового обязательного поля — заполнить его значениями по умолчанию на основе старых данных.
- При рефакторинге: объединение/разделение полей, перенос логики.
- Массовое исправление данных (например, нормализация email).

---

### ❌ Чего не делать

- Не используй `RunPython` вместо стандартных операций (`AddField`, `RenameModel` и т.д.).
- Не пиши долгие операции без пагинации — можно упереться в таймаут или память.
- Не вызывай внешние API или файловую систему — миграции должны быть **идемпотентными и автономными**.

---

### ✅ Итог

- Data migration = `makemigrations --empty` + `RunPython`.
- Работай только с **historical models** через `apps.get_model()`.
- Обеспечь обратимость, если это возможно.
- Запускается как обычная миграция: `python manage.py migrate`.