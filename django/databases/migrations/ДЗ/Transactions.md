**Транзакции в миграциях Django — кратко и по делу**

### 🔒 По умолчанию

- **PostgreSQL и SQLite**: вся миграция выполняется **в одной транзакции** → если что-то сломается — всё откатится.
- **MySQL и Oracle**: **транзакций для DDL нет** → каждая операция применяется сразу, отката при ошибке **не будет**.
### Управление транзакциями

#### 1. **Отключить транзакцию для всей миграции**
```python
from django.db import migrations

class Migration(migrations.Migration):
    atomic = False
```
→ Полезно при:

- очень больших миграциях (например, массовое обновление данных),
- операциях, несовместимых с транзакциями (например, `CREATE INDEX CONCURRENTLY` в PostgreSQL).
#### 2. **Частичная транзакция внутри нетранзакционной миграции**

Если `atomic = False`, но нужно выполнить часть кода атомарно:
```python
from django.db import migrations, transaction

def my_data_migration(apps, schema_editor):
    with transaction.atomic():
        # этот блок — в отдельной транзакции
        MyModel.objects.update(status='new')
```
### ⚠️ Важно

- Не все операции с БД можно откатить — особенно в MySQL/Oracle.
- Если миграция **меняет схему и данные одновременно**, учитывайте поведение вашей СУБД.
- При `atomic = False` — **нет автоматического отката**, ошибки могут оставить БД в частично изменённом состоянии.
**Итог:**  
Django использует транзакции в миграциях там, где СУБД позволяет. Вы можете гибко управлять этим поведением через `atomic`.

**Транзакция** — это последовательный набор операций с базой данных, который выполняются **целиком (атомарно) или не выполняется вообще**.

### 🔑 Основные свойства (ACID):

- **A**tomicity (Атомарность): либо все действия выполнены, либо ни одно.
- **C**onsistency (Согласованность): БД переходит из одного корректного состояния в другое.
- **I**solation (Изолированность): параллельные транзакции не мешают друг другу.
- **D**urability (Долговечность): после успешного завершения изменения сохраняются даже при сбое.