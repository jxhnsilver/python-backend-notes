### Что такое initial migration?

Это **первая миграция** в приложении, которая создаёт **исходную структуру таблиц** (моделей) этого приложения.

Обычно:

- Одна миграция (`0001_initial.py`)
- Помечена флагом: `initial = True`
- Если флаг не указан — Django считает **первой миграцией в папке** (у которой нет зависимостей от других миграций **в том же приложении**) как initial.
Пример:
```python
class Migration(migrations.Migration):
    initial = True
    dependencies = []
    operations = [
        migrations.CreateModel(...),
    ]
```
> В редких случаях (например, циклические связи между моделями) может быть **несколько initial-миграций**.

### 🔹 Зачем нужен флаг `initial = True`?

Чтобы Django **особо обрабатывал** эту миграцию при использовании флага `--fake-initial`.

---
### 🔹 Что делает `migrate --fake-initial`?

Этот режим нужен, **если таблицы уже существуют в БД**, но миграций ещё нет (например, вы добавляете миграции в старый проект).

Как работает:

1. Django **проверяет**, существуют ли в БД:
    - таблицы, которые создаёт `CreateModel`,
    - колонки, которые добавляет `AddField`.
2. Если всё есть — **не выполняет SQL**, а просто **записывает миграцию как применённую** в `django_migrations` («фейковое» применение).
3. Если чего-то не хватает — **падает с ошибкой** (не применяет и не фейкает).

> Без `--fake-initial` initial-миграции работают как обычные: пытаются создать таблицы, и если они уже есть — будет ошибка.

### 🔹 Когда это реально нужно?

- Вы **наследуете legacy-проект** без миграций, но с рабочей БД.
- Вы **вручную создали таблицы** (например, через SQL) и теперь хотите подключить систему миграций Django.
- Вы **клонируете прод-базу** локально и не хотите пересоздавать структуру, а только «привязать» существующие таблицы к миграциям.

---
### 🔹 Важные нюансы

- `--fake-initial` **работает только с initial-миграциями**.
- Он **не проверяет типы колонок, ограничения, индексы** — только факт существования таблицы/колонки с нужным именем.
- Если миграция делает что-то кроме `CreateModel`/`AddField` (например, `RunPython`) — `--fake-initial` **игнорирует это**, и такой код **не выполнится**.

---
### ✅ Итог

- **Initial migration** — первая миграция, создающая модели приложения.
- Флаг `initial = True` (или позиция первой в папке) помечает её как таковую.
- `--fake-initial` позволяет **пропустить создание таблиц**, если они уже есть — полезно при подключении миграций к существующей БД.