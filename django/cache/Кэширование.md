**Кэш (кеширование)** — это временное хранение результатов **дорогих** (по времени, ресурсам) операций, чтобы при повторном запросе **не выполнять их заново**, а вернуть готовый результат.
### Где используется (примеры из backend)

#### 1. **Кэширование ответа view (Django)**
```python
from django.views.decorators.cache import cache_page

@cache_page(60 * 15)  # кэшировать 15 минут
def expensive_view(request):
    # Медленный запрос: 1000 записей из БД + сложная обработка
    data = HeavyModel.objects.all()
    return JsonResponse({"count": len(data)})
```
→ Первый запрос: 2 секунды.  
→ Следующие 15 минут: 20 мс (отдаётся из кэша).
#### 2. **Кэширование результата функции**
```python
from django.core.cache import cache

def get_top_products():
    key = "top_products"
    result = cache.get(key)
    if result is None:
        result = list(Product.objects.filter(...).order_by("-sales")[:10])
        cache.set(key, result, timeout=3600)  # 1 час
    return result
```
→ При первом вызове — запрос в БД.  
→ При последующих — данные из кэша (Redis/Memcached).
#### 3. **Кэширование шаблонного фрагмента**
```html
{% load cache %}
{% cache 600 sidebar_user_{{ user.id }} %}
    <!-- Медленный виджет: последние действия пользователя -->
    {{ user.recent_actions }}
{% endcache %}
```
→ Для каждого пользователя своя версия, живёт 10 минут.

### Почему это важно

- **БД не грузится** повторяющимися запросами.
- **Сервер отвечает быстрее** → лучше UX и выше пропускная способность.
- **Экономия ресурсов**: CPU, RAM, I/O.
### Главное ограничение

Кэш **временный** и **может устареть**.  
→ Нужно правильно **управлять временем жизни (TTL)** или **инвалидировать** при изменении данных:
```python
# После обновления продукта — сбросить кэш
cache.delete("top_products")
```

### **Кэш-ключи (cache keys) — критически важно**

Кэш работает по принципу **ключ → значение**.  
Если ключ **одинаковый**, будет возвращён **тот же результат**, даже если данные изменились.

✅ **Правильно** — уникальный ключ для каждого контекста:
```python
key = f"product_{product_id}_lang_{user_lang}"
```
❌ **Ошибка** — общий ключ:
```python
key = "product"  # Все пользователи получат один и тот же продукт!
```
### **TTL (время жизни) vs инвалидация**

Есть два подхода:

- **TTL (time-to-live)** — автоматически удалять через N секунд  
    → Просто, но может отдавать устаревшие данные.
- **Инвалидация** — удалять вручную при изменении данных  
    → Точнее, но требует логики:
```python
def update_product(product_id, data):
    Product.objects.filter(id=product_id).update(**data)
    cache.delete(f"product_{product_id}")
```
### **Кэш и сериализация**

Кэш-хранилища (Redis, Memcached) хранят **байты**, а не Python-объекты.  
Django автоматически **сериализует/десериализует** через `pickle` (по умолчанию).

**Проблема**: если ты изменишь структуру модели — старый кэш может сломаться.  
→ Лучше кэшировать **словари или сериализованные данные**, а не целые модели:
```python
# Хорошо
cache.set("user_123", {"name": "Alice", "email": "..."})

# Рискованно
cache.set("user_123", User.objects.get(id=123))  # pickle-объект
```
### **Когда НЕ кэшировать**

- Персональные данные (личный кабинет, история заказов) — легко ошибиться и показать данные другого пользователя.
- Данные, которые **меняются чаще**, чем живёт кэш.
- Страницы с **побочными эффектами** (например, POST-запрос на оплату — никогда не кэшируй!).

## ✅ 1. Кэширование результата тяжёлого метода (сервисный слой)

**Кейс**: Получение статистики пользователя — дорогой SQL-запрос.
```python
# services.py
from django.core.cache import cache
from .models import Order

def get_user_stats(user_id: int) -> dict:
    key = f"user_stats:{user_id}"
    stats = cache.get(key)
    if stats is None:
        # Тяжёлая логика
        total_orders = Order.objects.filter(user_id=user_id).count()
        total_spent = Order.objects.filter(user_id=user_id).aggregate(
            total=models.Sum("amount")
        )["total"] or 0

        stats = {
            "total_orders": total_orders,
            "total_spent": float(total_spent),
        }
        cache.set(key, stats, timeout=60 * 10)  # 10 минут
    return stats
```
## ✅ 2. Инвалидация кэша при изменении данных

**Кейс**: После изменения заказа — сбросить кэш статистики.
```python
# models.py или signals.py
from django.db.models.signals import post_save
from django.dispatch import receiver
from django.core.cache import cache
from .models import Order

@receiver(post_save, sender=Order)
def invalidate_user_stats_cache(sender, instance, **kwargs):
    cache.delete(f"user_stats:{instance.user_id}")
```
Или вручную в view после сохранения:
```python
order.save()
cache.delete(f"user_stats:{order.user_id}")
```
---

## ✅ 3. Кэширование всего ответа view (для публичных страниц)
**Кейс**: Страница "ТОП товаров" — меняется редко.
```python
# views.py
from django.views.decorators.cache import cache_page
from django.views.decorators.vary import vary_on_headers

@cache_page(60 * 60)  # 1 час
@vary_on_headers("Accept-Language")  # разный кэш для разных языков
def top_products_view(request):
    products = Product.objects.filter(is_active=True).order_by("-rating")[:20]
    return render(request, "products/top.html", {"products": products})
```
`@vary_on_headers` — критически важно, если контент зависит от заголовков (язык, устройство и т.п.).

---
## ✅ 4. Кэширование фрагмента шаблона (часто — для виджетов)
**Кейс**: Блок "последние отзывы" в сайдбаре.
```django
<!-- base.html -->
{% load cache %}
{% cache 900 recent_reviews user.id %}
  <div class="reviews">
    {% for review in user.recent_reviews.all|slice:":3" %}
      <p>{{ review.text|truncatewords:15 }}</p>
    {% endfor %}
  </div>
{% endcache %}
```
💡 Ключ включает `user.id` — чтобы у каждого свой кэш.  
⚠️ Не забудь, что `recent_reviews` должен быть **методом модели или менеджера**, а не сырым `related_name` без лимита!
## Что НЕ делать (типичные ошибки джуниоров)
```python
# ❌ Плохо: общий ключ — все пользователи получат одни данные
cache.set("user_profile", profile_data)

# ❌ Плохо: кэширование целой модели (pickle-риск при миграциях)
cache.set(f"profile:{user_id}", user_model_instance)

# ❌ Плохо: без TTL — кэш может жить вечно
cache.set(key, data)  # timeout=не указан → по умолчанию None (forever)
```
## Настройка (settings.py) — рекомендуемый вариант для dev/prod
```python
# settings.py
CACHES = {
    "default": {
        "BACKEND": "django.core.cache.backends.redis.RedisCache",
        "LOCATION": os.environ.get("REDIS_URL", "redis://127.0.0.1:6379/1"),
        "OPTIONS": {
            "MAX_ENTRIES": 1000,  # защита от неограниченного роста
        }
    }
}
```
### Главное

Хороший кэш в Django:

- **Имеет понятный, уникальный ключ**
- **Имеет TTL или механизм инвалидации**
- **Хранит простые данные (dict, str, int)** — не модели
- **Легко отключается** (для отладки)
- **Не кэширует приватные/изменяющие данные**
### Вывод

Кэширование — **практический инструмент оптимизации**, а не абстракция.  
Если операция **часто вызывается** и **редко меняет результат** — её нужно кэшировать.  
Если данные **всегда разные** (например, личный кабинет в реальном времени) — кэшировать **не стоит**.