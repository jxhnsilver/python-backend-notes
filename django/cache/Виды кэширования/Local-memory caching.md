**Local-memory caching в Django** — это **встроенный кэш, который хранит данные в оперативной памяти самого процесса Python**, в котором запущено твоё Django-приложение.

---
## 🔑 Суть

- Это **самый простой кэш**: не требует Redis, Memcached, файлов — работает «из коробки».
- Данные хранятся в **обычном Python-словаре** внутри процесса.
- Используется **по умолчанию**, если ты не настроил `CACHES` в `settings.py`.

## Как настроить
```python
CACHES = {
    "default": {
        "BACKEND": "django.core.cache.backends.locmem.LocMemCache",
        "LOCATION": "my_cache",  # опционально, только если несколько кэшей
    }
}
```
Если у тебя **один кэш** — `LOCATION` можно не указывать.
## Ключевые особенности (и ограничения)

### 1. **Кэш привязан к процессу**

- Если ты запускаешь Django через **Gunicorn, Uvicorn, Daphne** — создаётся **несколько воркеров (процессов)**.
- **Каждый процесс имеет свой собственный кэш.**
- → Запросы, обрабатываемые разными воркерами, **не видят кэш друг друга**.

> Пример:
> 
> - Воркер 1 закэшировал `user_123`.
> - Воркер 2 делает `cache.get("user_123")` → получает `None`.  
>     → **Кэш не работает как единое хранилище.**

### 2. **Данные исчезают при перезапуске**

- Кэш живёт **только пока жив процесс**.
- При остановке/перезапуске сервера — **весь кэш теряется**.

### 3. **LRU-стратегия**

- При достижении лимита (`MAX_ENTRIES`) удаляются **самые старые** записи.
- Лимит по умолчанию: `300` записей (можно изменить через `OPTIONS`).

### 4. **Потокобезопасен**

- Безопасен для использования в многопоточных режимах (например, с `mod_wsgi` в потоках).
- Но **не помогает при многопроцессной архитектуре** (Gunicorn и т.д.).

## ✅ Когда использовать?
| Сценарий                                              | Подходит?                                      |
| ----------------------------------------------------- | ---------------------------------------------- |
| **Локальная разработка**                              | ✅ Да — быстро, ничего не нужно запускать       |
| **Тесты**                                             | ✅ Да — изолирован, не требует внешних сервисов |
| **Продакшен с одним процессом**                       | ⚠️ Теоретически да, но редко                   |
| **Продакшен с Gunicorn/Uvicorn (несколько воркеров)** | ❌ **Нет** — кэш не общий                       |
| **Высоконагруженный сайт**                            | ❌ Нет — нет масштабирования                    |
## 🆚 LocMemCache vs Redis/Memcached

| Критерий                 | LocMemCache                       | Redis / Memcached           |
| ------------------------ | --------------------------------- | --------------------------- |
| Требует внешних сервисов | ❌ Нет                             | ✅ Да                        |
| Общий для всех воркеров  | ❌ Нет                             | ✅ Да                        |
| Переживает перезапуск    | ❌ Нет                             | ✅ Redis (с RDB/AOF)         |
| Производительность       | ✅ Очень высокая (внутри процесса) | ✅ Высокая (сетевой оверхед) |
| Пригоден для продакшена  | ❌ Нет                             | ✅ Да                        |
## Рекомендация

- **В разработке** — можно использовать `LocMemCache` (или даже не настраивать `CACHES` вообще).
- **В продакшене** — **всегда используй Redis** (даже в Docker — это 2 команды).
- Если вдруг используешь `LocMemCache` в продакшене — убедись, что у тебя **ровно один воркер**, и ты **принимаешь риск потери кэша**.
## ✅ Вывод

> **`LocMemCache` — это временный, изолированный кэш для одного процесса.**  
> Он **удобен для разработки и тестов**, но **не подходит для реальных проектов**,  
> потому что **не масштабируется на несколько процессов** и **не обеспечивает согласованности**.