## ❓ Проблема: жёсткая привязка к кэшу

Когда ты пишешь:
```python
@cache_page(60 * 15)
def my_view(request):
    ...
```
— ты **навсегда связываешь логику view с кэшированием**. Это плохо, потому что:

1. **Нарушается разделение ответственности**:  
    View отвечает за бизнес-логику, а **кэширование — это инфраструктурная деталь**.
2. **Нельзя переиспользовать view без кэша**:  
    Хочешь использовать ту же view в другом проекте, где кэш не нужен? — **придётся копировать код и удалять декоратор**.
3. **Сложно тестировать**:  
    В тестах ты не можешь легко «отключить» кэш — он вшит в функцию.
4. **Нарушается принцип DIP (Dependency Inversion)**:  
    Логика зависит от детали (кэш), а не от абстракции.

## ✅ Решение: кэширование в URLconf

Переноси применение декоратора в файл маршрутов (`urls.py`):
```python
# views.py — чистая логика
def my_view(request, code):
    return render(request, "page.html", {"data": expensive_calculation(code)})
```

```python
# urls.py — здесь решаем, кэшировать или нет
from django.views.decorators.cache import cache_page

urlpatterns = [
    path("foo/<int:code>/", cache_page(60 * 15)(my_view)),
]
```
Обрати внимание: `cache_page(60*15)` **вызывается**, возвращает декоратор, который **применяется к `my_view`**.

## Преимущества такого подхода

| Преимущество             | Объяснение                                                         |
| ------------------------ | ------------------------------------------------------------------ |
| **View остаётся чистой** | Она не знает о кэше — только делает свою работу.                   |
| **Гибкость**             | В одном проекте — кэшируем, в другом — нет.                        |
| **Тестируемость**        | В тестах можно импортировать `my_view` напрямую — без кэша.        |
| **Соблюдение SOLID**     | Зависимость от кэша вводится **снаружи** (в URLconf), а не внутри. |
## Когда использовать?

- **Всегда, если view может быть переиспользована** (публичный API, библиотека views).
- **В продакшен-проектах**, где важна чистая архитектура.
- **На стажировке** — покажет, что ты понимаешь принципы проектирования.

---

## 🛠 Как это работает технически?

`cache_page(timeout)` — это **фабрика декораторов**.  
Она возвращает функцию-декоратор, которую можно применить к любой view:
```python
# Эквивалентны:
@cache_page(900)
def my_view(...): ...

# и
cached_view = cache_page(900)(my_view)
```
В URLconf ты просто передаёшь **уже «обёрнутую» view** в `path()`.
## ✅ Вывод

> **Кэширование — это не часть бизнес-логики, а инфраструктурное решение.**  
> Поэтому его **нужно применять на границе входа** — в URLconf, а не внутри view.

Этот подход:

- делает код **более чистым и переиспользуемым**,
- соответствует **принципам SOLID и DIP**,
- ожидается в **профессиональной backend-разработке**.
