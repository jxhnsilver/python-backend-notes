# Что такое Redis?

**Redis (REmote DIctionary Server)** — это **высокопроизводительное in-memory хранилище данных**, которое работает как **сетевой key-value сервер**.
## Ключевые свойства

|Свойство|Объяснение|
|---|---|
|**In-memory**|Все данные хранятся в RAM → доступ за **микросекунды**|
|**Key-value**|Основная модель: `SET key value`, `GET key`|
|**Поддержка структур**|Не только строки, а: **строки, хэши, списки, множества, сортированные множества**|
|**Опциональная персистентность**|Может сохранять данные на диск (RDB-снапшоты, AOF-лог)|
|**Сетевой сервис**|Работает как **отдельный демон**, к которому подключаются клиенты по TCP|
## **Во время работы — в RAM (основное хранилище)**

Redis держит всё в памяти процесса. Для каждого типа данных — своя эффективная структура:

| Тип Redis      | Внутренняя структура (в RAM)                         | Зачем                               |
| -------------- | ---------------------------------------------------- | ----------------------------------- |
| **STRING**     | Просто байтовая строка (SDS — Simple Dynamic String) | Быстрый доступ, длина известна      |
| **HASH**       | Хэш-таблица (или zipmap для малых данных)            | Быстрый lookup по полю              |
| **LIST**       | Связный список (quicklist — список зип-массивов)     | Эффективные `LPUSH`/`RPOP`          |
| **SET**        | Хэш-таблица (или intset для целых чисел)             | Быстрая проверка `SISMEMBER`        |
| **SORTED SET** | Skip list + хэш-таблица                              | Быстрая сортировка и поиск по score |
## Основные типы данных и примеры использования в backend

### 1. **STRING** — простое значение
```bash
SET session:abc123 "user_id=123;theme=dark"
GET session:abc123
```
✅ **Сессии**, **кэш HTML**, **счётчики (INCR)**
### 2. **HASH** — объект как набор полей
```bash
HSET user:123 name "Alice" email "a@example.com" role "admin"
HGET user:123 name
```
✅ **Профили пользователей**, **настройки**, **кэш моделей (без связей)**
### 3. **LIST** — упорядоченный список
```bash
LPUSH notifications:123 "Новое сообщение"
RPOP notifications:123
```
✅ **Очереди задач**, **лента событий**, **стек вызовов**
### 4. **SET** — неупорядоченное уникальное множество
```bash
SADD online_users "user1" "user2" "user3"
SISMEMBER online_users "user1"
```
✅ **Уникальные теги**, **участники чата**, **онлайн-статус**
### 5. **SORTED SET** — множество с рейтингом
```bash
ZADD leaderboard 100 "player1" 250 "player2"
ZREVRANGE leaderboard 0 9 WITHSCORES  # Топ-10
```
## Redis в Django: кэширование

### Настройка (`settings.py`)
```python
CACHES = {
    "default": {
        "BACKEND": "django.core.cache.backends.redis.RedisCache",
        "LOCATION": "redis://127.0.0.1:6379/1",  # база 1
        "TIMEOUT": 300,
        "KEY_PREFIX": "myapp",
    }
}
```
### Использование
```python
from django.core.cache import cache

# Кэш результата
def get_leaderboard():
    key = "leaderboard_v1"
    data = cache.get(key)
    if data is None:
        data = _calculate_leaderboard()  # тяжёлая логика
        cache.set(key, data, timeout=60)  # 1 минута
    return data
```
В кэше хранится **сериализованный объект** (по умолчанию `pickle`, лучше — JSON).

## Репликация в Redis

### Зачем нужна?

- **Отказоустойчивость**: если мастер упадёт — реплика примет на себя нагрузку.
- **Масштабирование чтения**: чтение можно распределять между репликами.

### Как работает?

- **Мастер (leader)**: принимает **запись**.
- **Реплики (replicas)**: получают копию данных от мастера → отвечают на **чтение**.

### Настройка в Django
```python
CACHES = {
    "default": {
        "BACKEND": "django.core.cache.backends.redis.RedisCache",
        "LOCATION": [
            "redis://master:6379",   # запись
            "redis://replica1:6379", # чтение
            "redis://replica2:6379", # чтение
        ],
    }
}
```
→ Django **автоматически** отправляет:

- `SET`, `DELETE` → на **первый сервер** (мастер),
- `GET` → на **случайную реплику**.
## Безопасность

- **По умолчанию Redis без пароля** → **никогда не запускай его на публичном IP**.
- В продакшене обязательно:
```conf
# redis.conf
requirepass your_strong_password
bind 10.0.0.0  # только внутренняя сеть
```
В Django:
```python
"LOCATION": "redis://:password@10.0.0.10:6379"
```
## Redis vs Memcached

| Критерий        | Redis                                      | Memcached                     |
| --------------- | ------------------------------------------ | ----------------------------- |
| Типы данных     | Строка, хэш, список, множество, sorted set | Только строка                 |
| Персистентность | Да (RDB, AOF)                              | Нет                           |
| Репликация      | Да                                         | Нет (только шардинг клиентом) |
| Использование   | Кэш, очередь, рейт-лимит, сессии           | Только кэш                    |
| Скорость        | Очень высокая                              | Чуть выше (проще архитектура) |
| Популярность    | Стандарт в новых проектах                  | Legacy                        |
## Продвинутые сценарии в backend

### 1. **Rate limiting (ограничение запросов)**
```python
def is_rate_limited(user_id, limit=100, window=3600):
    key = f"rate_limit:{user_id}"
    current = cache.get(key)
    if current is None:
        cache.set(key, 1, timeout=window)
        return False
    if current >= limit:
        return True
    cache.incr(key)  # INCR в Redis
    return False
```
### 2. **Очередь задач (простая)**
```python
# Отправка
cache.lpush("task_queue", json.dumps({"type": "send_email", "to": "..."})

# Обработка (воркер)
task = cache.rpop("task_queue")
if task:
    handle_task(json.loads(task))
```

## 1. **Один Redis-сервер (standalone)** → ❌ нет распределения

- Один процесс, один инстанс.
- Вся нагрузка (чтение + запись) идёт на него.
- **Масштабирование только вертикальное** (больше RAM, CPU).
- **Нет отказоустойчивости** — если упадёт, кэш пропадёт.

> Это **стандартная настройка для разработки и небольших проектов**.

---

## 🔹 2. **Мастер-реплика (Replication)** → ✅ распределение **чтения**

- **Мастер (master/leader)**: принимает **запись**.
- **Реплики (replicas/slaves)**: копируют данные с мастера → отвечают на **чтение**.

### Как распределяется нагрузка?

- **Запись** — всегда на **один мастер**.
- **Чтение** — можно направлять на **любую реплику**.

### В Django это работает автоматически:
```python
CACHES = {
    "default": {
        "BACKEND": "django.core.cache.backends.redis.RedisCache",
        "LOCATION": [
            "redis://master:6379",   # ← сюда идёт запись
            "redis://replica1:6379", # ← сюда — чтение
            "redis://replica2:6379", # ← и сюда — чтение
        ],
    }
}
```
→ Django **сам отправляет `GET` на реплики, а `SET/DELETE` — на мастер**.

✅ **Это распределение нагрузки на чтение.**  
❌ **Запись не масштабируется** — всё идёт на один мастер.

## Рекомендации для джуниора

1. **Используй Redis как кэш** — это основное применение.
2. **Не кэшируй модели Django** — только словари.
3. **Всегда указывай `TIMEOUT`**.
4. **В продакшене — пароль + внутренняя сеть**.
5. **Для сложных сценариев (очереди, рейт-лимиты) — изучай команды Redis напрямую**, но начинай с Django-кэша.

---

## ✅ Вывод

> **Redis — это не просто кэш, а универсальный in-memory datastore**, который:
> 
> - Ускоряет приложение через кэширование,
> - Позволяет реализовать сессии, очереди, рейты,
> - Поддерживает отказоустойчивость через репликацию,
> - Стал стандартом в современной backend-разработке.