## 1. **Безопасность — главная разница**

|                                                 | **JSON**                                                      | **pickle**                                                                                         |
| ----------------------------------------------- | ------------------------------------------------------------- | -------------------------------------------------------------------------------------------------- |
| **Что делает при десериализации?**              | Только **парсит структуру**: объекты, массивы, строки, числа. | **Выполняет произвольный Python-код**, заложенный в данные.                                        |
| **Может привести к RCE?**                       | ❌ Нет.                                                        | ✅ **Да.** Если злоумышленник подсунет вредоносный `pickle` — при `loads()` **выполнится его код**. |
| **Можно использовать с недоверенными данными?** | ✅ Да.                                                         | ❌ **Никогда.**                                                                                     |
### Пример уязвимости `pickle`:
```python
import pickle
import os

# Злоумышленник создаёт такой "кэш"
class Exploit:
    def __reduce__(self):
        return (os.system, ("rm -rf /",))

malicious = pickle.dumps(Exploit())

# При загрузке — выполняется опасная команда!
pickle.loads(malicious)  # ← Удаляет всё на сервере
```
**JSON такого не может — он не знает, что такое `os.system`.**
## 2. **Поддерживаемые типы данных**
|                     | **JSON**                                              | **pickle**                                                                                        |
| ------------------- | ----------------------------------------------------- | ------------------------------------------------------------------------------------------------- |
| **Поддерживает**    | `dict`, `list`, `str`, `int`, `float`, `bool`, `None` | **Любой Python-объект**: классы, функции, экземпляры, `datetime`, `Decimal`, модели Django и т.д. |
| **Пример проблемы** | Не может сериализовать `datetime` без преобразования  | Сериализует `User.objects.get(1)` «как есть»                                                      |
→ `pickle` кажется удобнее, но **это ловушка**: закэшировал модель — сломалось после миграции.
## 3. **Совместимость**
|                        | **JSON**                                              | **pickle**                                |
| ---------------------- | ----------------------------------------------------- | ----------------------------------------- |
| **Языки**              | Универсальный стандарт: Python, JS, Go, Java, Rust... | **Только Python**                         |
| **Версии Python**      | Работает везде одинаково                              | Может не работать между Python 3.8 ↔ 3.12 |
| **Человекочитаемость** | ✅ Да: `{"name": "Alice"}`                             | ❌ Нет: `b'\x80\x04\x95...'`               |
## 4. **Производительность**

| Операция                           | **JSON**                       | **pickle**                                 |
| ---------------------------------- | ------------------------------ | ------------------------------------------ |
| **Сериализация (простые данные)**  | Быстрее или сравнимо           | Чуть медленнее                             |
| **Сериализация (сложные объекты)** | Требует ручного преобразования | Быстрее (автоматически)                    |
| **Десериализация**                 | Очень быстрая                  | Медленнее (из-за безопасности и сложности) |
Но: **в 99% случаев кэшируют простые данные** (`dict`, `list`) → **JSON быстрее и безопаснее**.
## Сводная таблица

| Критерий                 | **JSON**            | **pickle**           |
| ------------------------ | ------------------- | -------------------- |
| **Безопасность**         | ✅ Полная            | ❌ Уязвим к RCE       |
| **Типы данных**          | Только примитивы    | Любые объекты Python |
| **Совместимость**        | ✅ Мультиязыковая    | ❌ Только Python      |
| **Читаемость**           | ✅ Да                | ❌ Нет                |
| **Скорость (для кэша)**  | ✅ Быстрее           | ⚠️ Медленнее         |
| **Использование в кэше** | ✅ **Рекомендуется** | ❌ **Избегать**       |
## Практический вывод для backend-разработчика

> **Всегда кэшируй только простые данные (словари, списки).**  
> **Всегда используй JSON вместо `pickle`.**

### Как в Django:
```python
# Хорошо
cache.set("user_123", {"id": 123, "name": "Alice"})

# Плохо
cache.set("user_123", User.objects.get(id=123))  # pickle + модель = риск
```
Если хочешь **явно использовать JSON** (а не полагаться на `pickle` по умолчанию), создай кастомный бэкенд (см. примеры выше).

## ✅ Ты абсолютно прав: и JSON, и pickle **в итоге превращаются в байты** перед записью в Redis/Memcached/файлы.

**НО** — **читаемость всё равно важна**, и вот почему:

---
## 🔍 Читаемость — это про **диагностику и отладку**, а не про формат хранения

Когда данные записаны в Redis, они **действительно лежат как байты**.  
Но **ты можешь подключиться к Redis напрямую** (например, через `redis-cli`) и **увидеть содержимое**.
### Пример:
#### С JSON:
```bash
$ redis-cli
127.0.0.1:6379> GET user_123
"{\"id\": 123, \"name\": \"Alice\"}"
```
→ Ты **сразу видишь**, что внутри: структуру, значения, ошибки.
#### С pickle:
```bash
127.0.0.1:6379> GET user_123
"\x80\x04\x95#\x00\x00\x00\x00\x00\x00\x00}\x94(\x8c\x02id\x94K{\x8c\x04name\x94\x8c\x05Alice\x94u."
```
→ Это **бинарный мусор**. Ты **не поймёшь**, что там — без десериализации в Python.\
## Зачем это нужно на практике?

| Сценарий                         | С JSON                                  | С pickle                                          |
| -------------------------------- | --------------------------------------- | ------------------------------------------------- |
| **Проверить, что в кэше**        | Открыл `redis-cli` — сразу видно данные | Нужно писать скрипт на Python, чтобы расшифровать |
| **Найти битые данные**           | Видишь `{"name": null}` — понятно       | Видишь байты — неясно, ошибка или норма           |
| **Поддержка в продакшене**       | DevOps может помочь без знания Python   | Только разработчик на Python может разобраться    |
| **Интеграция с другим сервисом** | Можно прочитать на любом языке          | Только Python                                     |
## Главное понимание

> **Да, оба формата хранятся как байты.**  
> **Но JSON — это байты, полученные из читаемой текстовой строки.**  
> **А pickle — это байты, полученные из бинарного протокола.**

Именно поэтому **JSON остаётся читаемым даже в сыром виде**, а pickle — нет.

## Да, в Redis/Memcached данные **хранятся как байты**.

Но **"читаемость" зависит от того, какие именно байты**:

- Если байты — это **закодированная UTF-8 строка**, содержащая JSON → они **читаемы как текст**.
- Если байты — это **бинарный протокол pickle** → они **нечитаемы**.
**UTF-8 строка** — это **текст, закодированный в формате UTF-8**, который можно **хранить, передавать и читать как последовательность байтов**, при этом **сохраняя все символы** (латиница, кириллица, эмодзи и т.д.).