## Главное различие

> **Memcached — это внешняя система (программа).**  
> **Django cache — это API (интерфейс) внутри Django.**

Они **не конкурируют**, а **работают вместе**:

- **Django cache** — это **единый интерфейс** для работы с кэшем: `cache.set()`, `cache.get()`.
- **Memcached** — это **один из возможных способов хранения** данных, которые ты передаёшь через этот интерфейс.
---
## 🧩 Аналогия

Представь, что:

- **Django cache** — это **розетка** в стене (универсальный интерфейс для подключения).
- **Memcached, Redis, файлы, память** — это **разные приборы**, которые можно в неё воткнуть: утюг, чайник, лампа.

Ты пишешь код **только под розетку** (`cache.set(...)`),  
а **что именно работает внутри** — решает настройка `CACHES` в `settings.py`.
## Конкретика

### Django cache — это:

- Модуль `django.core.cache`
- Функции: `cache.set(key, value)`, `cache.get(key)`
- Декораторы: `@cache_page`, `{% cache %}` в шаблонах
- **Абстракция над реальным хранилищем**
### Memcached — это:

- Отдельный **демон (сервис)**, запущенный в системе
- Хранит данные **в RAM**
- Принимает команды по сети: `set key val`, `get key`
- **Не имеет отношения к Django сам по себе**
---
## ⚙️ Как они связаны

В `settings.py` ты **говоришь Django**:

> «Используй Memcached как хранилище для кэша».

```python
CACHES = {
    "default": {
        "BACKEND": "django.core.cache.backends.memcached.PyMemcacheCache",
        "LOCATION": "127.0.0.1:11211",
    }
}
```
Теперь, когда ты вызываешь:
```python
from django.core.cache import cache
cache.set("my_key", "hello")
```
Django **внутри** подключается к Memcached и отправляет ему команду `set my_key hello`.
## Django cache vs Memcached — таблица

| Вопрос                                                | Ответ                                             |
| ----------------------------------------------------- | ------------------------------------------------- |
| **Что такое Django cache?**                           | API фреймворка для кэширования.                   |
| **Что такое Memcached?**                              | Внешний in-memory key-value store.                |
| **Можно ли использовать Django cache без Memcached?** | Да — с Redis, файлами, памятью и т.д.             |
| **Можно ли использовать Memcached без Django?**       | Да — из любого языка: Python, Node.js, PHP и т.д. |
| **Кто вызывает кого?**                                | Django cache **использует** Memcached как бэкенд. |

## Как работает `django.core.cache`

`django.core.cache` — это **единый программный интерфейс (API)** для кэширования.  
Он **не хранит данные сам**, а делегирует работу **настоящему хранилищу** — Redis, Memcached, файлам и т.д.

**Механизм:**

1. Ты вызываешь `cache.set("key", value)`.
2. Django смотрит в `settings.py` → находит `CACHES["default"]["BACKEND"]`.
3. Создаёт экземпляр соответствующего бэкенда (например, `RedisCache`).
4. Бэкенд **внутри использует официальную библиотеку** (`redis-py`, `pymemcache`) для отправки данных в Redis/Memcached.

→ Ты **работаешь с абстракцией**, а Django **под капотом общается с реальным сервисом**.

## Реальный пример из разработки

**Сценарий**:  
Ты разрабатываешь интернет-магазин. На главной странице — блок «ТОП-10 товаров». Он грузится медленно (сложный SQL + обработка).

### С `django.core.cache`:
```python
# services.py
from django.core.cache import cache

def get_top_products():
    key = "top_products_v1"
    products = cache.get(key)
    if products is None:
        products = _expensive_db_query()
        cache.set(key, products, timeout=60 * 15)  # 15 минут
    return products
```
**Настройка в `settings.py`:**
```python
# Разработка
CACHES = {"default": {"BACKEND": "...redis.RedisCache", "LOCATION": "redis://127.0.0.1:6379"}}

# Продакшен (через env)
CACHES = {"default": {"BACKEND": "...redis.RedisCache", "LOCATION": os.getenv("REDIS_URL")}}
```
**Что даёт:**

- При переходе с Redis на Memcached — **меняешь только `settings.py`**.
- Все вызовы `cache.get/set` **остаются без изменений**.
- TTL, сериализация, префиксы — управляются централизованно.
## ❌ Что будет, если использовать Redis напрямую
```python
# ПЛОХОЙ КОД
import redis
client = redis.Redis(host="...", port=6379)

def get_top_products():
    data = client.get("top_products_v1")  # возвращает bytes!
    if data is None:
        products = _expensive_db_query()
        client.setex("top_products_v1", 900, json.dumps(products))  # ручная сериализация
        return products
    return json.loads(data)  # ручная десериализация
```
### Возникнут проблемы:

|Проблема|Последствие|
|---|---|
|**Жёсткая привязка к Redis**|Невозможно перейти на Memcached без полного рефакторинга|
|**Ручная сериализация**|Ошибки типов, несовместимость при смене формата данных|
|**Нет централизованного TTL**|В одном месте 900 сек, в другом 1800 — неочевидное поведение|
|**Нет `KEY_PREFIX`**|Если два проекта используют один Redis — кэши конфликтуют|
|**Сложные тесты**|Приходится мокать `redis.Redis`, а не просто подменить бэкенд|
|**Нет поддержки `@cache_page` и `{% cache %}`**|Приходится изобретать велосипед для view и шаблонов|
## ✅ Главный вывод

> **`django.core.cache` — это уровень абстракции, который:**
> 
> - **Скрывает детали реализации** (Redis, Memcached и т.д.),
> - **Гарантирует единообразие** в работе с кэшем по всему проекту,
> - **Позволяет менять инфраструктуру без изменения бизнес-логики**.

Использовать Redis/Memcached напрямую — нарушать принцип **инверсии зависимостей (DIP)**: код начинает зависеть от конкретной реализации, а не от абстракции.

## Что говорит DIP (формулировка)

> 1. Модули верхнего уровня не должны зависеть от модулей нижнего уровня.
> 2. Оба должны зависеть от абстракций.
> 3. Абстракции не должны зависеть от деталей. Детали должны зависеть от абстракций.
## ✅ Как оба уровня зависят от абстракции

### 1. **Бизнес-логика (высокий уровень) зависит от абстракции**
```python
from django.core.cache import cache  # ← это абстракция

def get_user_profile(user_id: int):
    return cache.get(f"user:{user_id}")  # ← использует интерфейс
```
→ Твоя функция **не знает**, что внутри Redis или Memcached.  
→ Она **зависит только от контракта**: «есть метод `get(key)`».
✅ **Высокий уровень зависит от абстракции** — ✔️
### 2. **Детали (низкий уровень) тоже зависят от абстракции**

Вот как реализован, например, бэкенд Redis в Django:
```python
# django/core/cache/backends/redis.py (упрощённо)
from django.core.cache.backends.base import BaseCache  # ← АБСТРАКЦИЯ

class RedisCache(BaseCache):  # ← ДЕТАЛЬ
    def get(self, key):
        return self._redis_client.get(key)

    def set(self, key, value, timeout):
        return self._redis_client.setex(key, timeout, value)
```
- `RedisCache` — это **конкретная деталь** (низкий уровень).
- Он **наследуется от `BaseCache`** — абстрактного класса (интерфейса), определённого в Django.
- Таким образом, **реализация (деталь) обязана соответствовать абстракции**.
✅ **Низкий уровень (деталь) зависит от абстракции** — ✔️

> Обрати внимание: **абстракция (`BaseCache`) определена до и независимо от деталей**.  
> Redis-бэкенд **подстраивается под неё**, а не наоборот.

## 🔄 Схема зависимостей (стрелки = "зависит от")
```
get_user_profile()      RedisCache
          \                /
           \              /
            \            /
          django.core.cache (BaseCache) ← абстракция
```
→ **Оба — и бизнес-логика, и RedisCache — зависят от одной и той же абстракции.**  
→ **Абстракция не зависит ни от кого.**  
→ **Детали (RedisCache) зависят от абстракции**, а не наоборот.
Это и есть **чистый DIP**.

## Вывод

> **`django.core.cache` — это интерфейс.**  
> **Memcached — это реализация (бэкенд) этого интерфейса.**

Ты **всегда работаешь с `django.core.cache`**,  
а **Memcached (или Redis) — просто «движок», куда данные уходят**.

Это позволяет **менять кэш-систему без изменения кода**:  
сменил `BACKEND` в настройках — и всё заработало с Redis вместо Memcached.

---

✅ **Для джуниора:**  
Запомни:
Ты **никогда не пишешь код напрямую для Redis или Memcached**.  
Ты всегда пишешь **только через абстракцию Django**:
- **Memcached/Redis — это инфраструктура, которую настраивают отдельно.**
- Они **разного уровня**: один — часть фреймворка, другой — отдельный сервис.