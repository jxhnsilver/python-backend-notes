## 🔹 1. Семантическое отличие (главное!)

| Метод    | Назначение                                                                                  |
| -------- | ------------------------------------------------------------------------------------------- |
| **GET**  | **Получить** данные с сервера. Не должен изменять состояние.                                |
| **POST** | **Отправить** данные на сервер (создать, обновить, запустить действие). Изменяет состояние. |
### **GET — для получения данных (идемпотентный, безопасный)**

- **Назначение:** Получить ресурс, **не изменяя состояние сервера**
- **Параметры в URL** — логично, так как они:
    - Описывают **что** мы хотим получить (фильтры, поисковый запрос, ID)
    - Позволяют кэшировать **полный запрос** (URL + параметры)
    - Создают возможность делиться ссылкой (вся информация в адресной строке)
### **POST — для изменения данных (неидемпотентный)**

- **Назначение:** Создать/изменить ресурс, **изменяя состояние сервера**
- **Параметры в теле** — логично, так как:
    - Это **конфиденциальные данные** (пароли, платежная информация)
    - Это **большие объемы данных** (формы, загрузка файлов)
    - Это **сложные структуры** (JSON, XML)
    - **Не должны** быть частью публичной ссылки

## 🔹 2. Структура HTTP-запроса

### ✅ GET-запрос
```http
GET /search?q=python&lang=ru HTTP/1.1
Host: example.com
User-Agent: Mozilla/5.0
Accept: */*
```
- **Данные передаются в URL** (в query string: `?q=python&lang=ru`)
- **Тело (body) отсутствует** (по стандарту — не должно использоваться)
- Строка запроса: `GET /путь?параметры HTTP/1.1`
### ✅ POST-запрос
```http
POST /login HTTP/1.1
Host: example.com
Content-Type: application/x-www-form-urlencoded
Content-Length: 27
User-Agent: Mozilla/5.0

username=alice&password=secret
```
- **Данные передаются в теле запроса** (после пустой строки `\r\n\r\n`)
- Обязательны заголовки:
    - `Content-Type` — как закодированы данные,
    - `Content-Length` — размер тела в байтах.
## 🔹 3. Где передаются данные?

| Метод    | Место передачи данных    | Пример                              |
| -------- | ------------------------ | ----------------------------------- |
| **GET**  | В **URL** (query string) | `https://site.com/search?q=hello`   |
| **POST** | В **теле запроса**       | Тело: `username=alice&password=123` |
⚠️ В GET **нельзя** передать бинарные данные (например, файлы).  
В POST — можно (через `multipart/form-data`).
## 🔹 4. Заголовки: ключевые различия

| Заголовок        | GET                                     | POST                                |
| ---------------- | --------------------------------------- | ----------------------------------- |
| `Content-Type`   | ❌ Обычно отсутствует                    | ✅ Обязателен (если есть тело)       |
| `Content-Length` | ❌ Отсутствует                           | ✅ Обязателен                        |
| `Host`           | ✅ Есть                                  | ✅ Есть                              |
| Длина URL        | Ограничена (~2048 символов в браузерах) | Не ограничена (ограничено сервером) |