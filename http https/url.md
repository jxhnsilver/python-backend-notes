**URL (Uniform Resource Locator)** — это строка, которая указывает **уникальный адрес ресурса** в интернете.  
В REST API URL используется для доступа к данным (ресурсам).
### 🔹 Общий формат:
```
scheme://[userinfo@]host[:port][/path][?query][#fragment]
```
### 🔹 Компоненты URL (на примере):
```
https://api.example.com:443/v1/users?id=42&lang=ru
```

| Часть               | Значение                         | Обязательная?                                    | Передаётся на сервер?                                    |
| ------------------- | -------------------------------- | ------------------------------------------------ | ---------------------------------------- |
| **scheme/protocol** | `https` — протокол               | ✅ Да                                             | ❌ Нет (сервер знает по контексту)                        |
| **host**            | `api.example.com` — домен или IP | ✅ Да                                             | ✅ Да (в заголовке `Host`)                                |
| **port**            | `443` — порт                     | ❌ Нет (по умолчанию: 80 для http, 443 для https) | ✅ Косвенно (клиент подключается к нему)                  |
| **path**            | `/v1/users` — путь к ресурсу     | ❌ Нет                                            | ✅ Да (в строке запроса: `GET /v1/users`)                 |
| **query**           | `?id=42&lang=ru` — параметры     | ❌ Нет                                            | ✅ Да (в той же строке запроса)                           |
### 🔹 Кратко по каждой части:

1. **Scheme**  
    — Определяет протокол: `http`, `https`, `ftp`, `ws`, `mailto` и др.  
    — Указывает, как интерпретировать остальной URL.
2. **Host**  
    — Домен (`example.com`) или IP-адрес (`192.0.2.1`, `[2001:db8::1]` для IPv6).  
    — Обязателен для сетевых URL.
3. **Port**  
    — Указывает, к какому порту подключаться.  
    — Если не указан — используется порт по умолчанию для схемы.
4. **Path**  
    — Иерархический путь к ресурсу на сервере.  
    — Может быть пустым (тогда `/` подразумевается).
5. **Query (query string)**  
    — Набор параметров в формате `ключ=значение`, разделённых `&`.  
    — Используется для фильтрации, поиска, передачи данных.  
    — Пример: `?search=python&page=2`
### 🔹 Примеры валидных URL:

- `https://example.com`
- `https://example.com:8080/search?q=hello`
- `http://192.168.1.1/admin`
- `https://api.service.com/v1/data?format=json#result`