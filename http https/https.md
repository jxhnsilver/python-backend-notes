**HTTPS (HyperText Transfer Protocol Secure)** — это **безопасная версия HTTP**, в которой данные между клиентом (например, браузером) и сервером **шифруются** с помощью криптографического протокола **TLS** (ранее — SSL).
### 🔹 Основная цель HTTPS:

> **Защитить конфиденциальность, целостность и подлинность** данных при передаче по сети.

### 🔹 Чем HTTPS отличается от HTTP?

|Характеристика|HTTP|HTTPS|
|---|---|---|
|**Порт по умолчанию**|80|443|
|**Шифрование**|❌ Нет|✅ Да (TLS/SSL)|
|**Целостность**|❌ Может быть изменён|✅ Гарантируется|
|**Аутентификация**|❌ Нет|✅ Сервер подтверждает свою личность (через сертификат)|
|**SEO**|Хуже|Лучше (Google ранжирует выше)|
|**Скорость**|Чуть быстрее|Небольшие накладные расходы на handshake|
### 🔹 Как работает HTTPS?

1. **Клиент** (браузер) подключается к серверу по `https://example.com`.
2. Сервер отправляет свой **цифровой сертификат**, выданный доверенным **удостоверяющим центром (CA)**.
3. Браузер проверяет:
    - Действителен ли сертификат?
    - Соответствует ли домену?
    - Не отозван ли?
4. Если всё в порядке — клиент и сервер выполняют **TLS handshake**:
    - Договариваются о версии TLS и наборе шифров (cipher suite),
    - Обмениваются ключами (обычно через **ECDHE**),
    - Создают **секретный сессионный ключ**.
5. Далее **весь HTTP-трафик шифруется** этим ключом.

> 🔐 Даже если злоумышленник перехватит трафик — он не сможет прочитать или изменить данные.

### 🔹 Компоненты HTTPS

1. **HTTP** — сам протокол передачи данных.
2. **TLS (Transport Layer Security)** — протокол шифрования (SSL — его устаревшая версия).
3. **Цифровой сертификат** — файл, подтверждающий личность сервера. Содержит:
    - Публичный ключ сервера,
    - Имя домена,
    - Подпись от CA (например, Let's Encrypt, DigiCert).
### 🔹 Пример: как выглядит HTTPS в URL
```url
https://api.example.com/v1/users
```
- `https` — схема (указывает на использование TLS),
- всё остальное — как в обычном URL.
### 🔹 Почему HTTPS обязателен сегодня?

- **Безопасность пользователей** (логины, платежи, персональные данные),
- **Требования браузеров**: современные функции (геолокация, Service Workers) работают **только по HTTPS**,
- **SEO**: Google понижает сайты без HTTPS,
- **Конфиденциальность**: даже запросы к API должны быть защищены.
### 💡 Кратко:

> **HTTPS = HTTP + шифрование + аутентификация + целостность.**  
> Это стандарт современного веба. **Любой сайт без HTTPS считается ненадёжным.**