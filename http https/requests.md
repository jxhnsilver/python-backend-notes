Библиотка **`requests`** — это библиотека в Python для отправки HTTP-запросов. Она делает взаимодействие с веб-API, сайтами и серверами простым, читаемым и надёжным.

> ✅ Заменяет низкоуровневые модули вроде `urllib` — пишешь меньше кода, получаешь больше возможностей.

## 🔹 Основные возможности

- Отправка **GET**, **POST**, **PUT**, **DELETE** и других HTTP-методов
- Работа с **заголовками**, **куками**, **параметрами**, **формами**, **JSON**
- Автоматическая обработка **кодировок**
- Поддержка **сессий**, **аутентификации**, **прокси**, **таймаутов**
- Лёгкое чтение **ответа**: статус, заголовки, тело (текст или JSON)
## 🔹 Самые важные функции и примеры

### 1. **GET-запрос**
```python
import requests

# Простой GET
response = requests.get("https://httpbin.org/get")

# С параметрами (?key=value)
response = requests.get("https://httpbin.org/get", params={"q": "python", "lang": "ru"})

print(response.status_code)  # 200
print(response.json())       # автоматический парсинг JSON
```
⚠️ `params` — словарь, `requests` сам закодирует его в `?q=python&lang=ru`.
### 🔹 **Ключевые параметры (что чаще всего используешь)**

| Параметр  | Назначение                                                                        | Пример                                      |
| --------- | --------------------------------------------------------------------------------- | ------------------------------------------- |
| `url`     | Адрес сервера                                                                     | `"https://api.example.com/users"`           |
| `params`  | Параметры для **GET** (попадут в `?key=value`)                                    | `params={"q": "python"}`                    |
| `data`    | Тело запроса как **форма** (`application/x-www-form-urlencoded`)                  | `data={"user": "alice"}`                    |
| `json`    | Тело запроса как **JSON** (автоматически ставит `Content-Type: application/json`) | `json={"user": "alice"}`                    |
| `headers` | Свои заголовки                                                                    | `headers={"Authorization": "Bearer token"}` |
| `timeout` | Макс. время ожидания ответа (в секундах) — **обязательно указывай!**              | `timeout=10`                                |
### 🔹 **Чем `data` отличается от `json`?**

|                        | `data=...`                               | `json=...`                                              |
| ---------------------- | ---------------------------------------- | ------------------------------------------------------- |
| **Тип данных**         | Словарь → кодируется как `key=value&...` | Словарь → превращается в JSON-строку                    |
| **Content-Type**       | `application/x-www-form-urlencoded`      | `application/json`                                      |
| **Когда использовать** | Отправка HTML-форм                       | Работа с API (почти все современные API принимают JSON) |
