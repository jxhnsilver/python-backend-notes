# 🔹 HTTP метод GET

**GET** — это HTTP-метод, который используется для **получения данных с сервера**.

- **Безопасный**: не изменяет состояние сервера (только читает данные).
- **Идемпотентный**: повторный запрос не изменяет результат (результат будет тот же).
- Данные передаются через **URL** (путь + query-параметры).

---
## 📌 Основные моменты

1. **Цель метода**
    - Получить ресурс или список ресурсов с сервера.
        
2. **Передача данных**
    - Через URL-параметры (`?key=value`)
    - Тело запроса **не используется**
        
3. **Коды ответов**
    - `200 OK` — успешно, данные возвращены
    - `404 Not Found` — ресурс не найден
    - `401 Unauthorized` — нет авторизации
    - `500 Internal Server Error` — ошибка сервера

## 🖥️ Пример запроса GET

### Получить список пользователей
```
GET https://api.example.com/users
```

### Получить конкретного пользователя по ID
```
GET https://api.example.com/users/123
```

### С фильтрацией и сортировкой
```
GET https://api.example.com/users?age=25&sort=desc
```

### Пример ответа (JSON)
```json
[
  { 
	  "id": 123, 
	  "name": "Ilia", 
	  "email": "ilia@example.com" 
  },
  { 
	  "id": 124, 
	  "name": "Alex", 
	  "email": "alex@example.com" 
  }
]
```

## Итог для собеса

> **GET** — HTTP-метод для получения данных с сервера. Безопасный и идемпотентный. Данные передаются через URL, а ответ обычно приходит в формате JSON.