# 🔹 HTTP метод POST

**POST** — это HTTP-метод, который используется для **создания нового ресурса на сервере** или выполнения действия, которое изменяет состояние сервера.

- **Не безопасный**: изменяет состояние сервера.
- **Не идемпотентный**: повторный запрос может создать несколько ресурсов или изменить состояние несколько раз.
- Данные передаются **в теле запроса (Body)**, обычно в формате JSON.

---
## 📌 Основные моменты

1. **Цель метода**
    - Создать новый ресурс (например, нового пользователя, заказ, продукт).
    - Передать данные для обработки на сервер.
        
2. **Передача данных**
    - Тело запроса (`Body`)
    - Формат: JSON, XML, Form Data и др.
    - Заголовок `Content-Type` указывает формат:
    ```
      Content-Type: application/json
    ```
3. **Коды ответа**
	- `201 Created` — ресурс успешно создан
	- `400 Bad Request` — ошибка в запросе
	- `401 Unauthorized` — нет авторизации
	- `500 Internal Server Error` — ошибка сервера

## Пример запроса POST

### Создать нового пользователя
```
POST https://api.example.com/users
Content-Type: application/json
```

### Тело запроса (JSON):
```json
{
  "name": "Ilia",
  "email": "ilia@example.com",
  "age": 25
}
```

### Ответ сервера:
```json
{
  "id": 123,
  "name": "Ilia",
  "email": "ilia@example.com",
  "createdAt": "2025-09-30T10:00:00Z"
}
```

## Итог для собеса

> **POST** — HTTP-метод для создания нового ресурса или отправки данных на сервер. Не безопасный и не идемпотентный. Данные передаются в теле запроса, обычно в JSON.