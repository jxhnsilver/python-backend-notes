# 🔹 HTTP метод PUT

**PUT** — это HTTP-метод, который используется для **полного обновления существующего ресурса** на сервере или его создания, если ресурс ещё не существует.

- **Не безопасный**: изменяет состояние сервера.
- **Идемпотентный**: повторный запрос с одинаковыми данными **не изменяет результат дополнительно**.
- Данные передаются **в теле запроса (Body)**, чаще всего в формате JSON.
## 📌 Основные моменты

1. **Цель метода**
    - Полностью заменить ресурс новыми данными.
    - Создать ресурс, если его нет (в некоторых реализациях REST).
        
2. **Передача данных**
    - Тело запроса (`Body`)
    - Формат: JSON
    - Заголовок `Content-Type` указывает формат:
```
Content-Type: application/json
```

3. **Коды ответа**
	- `200 OK` — ресурс успешно обновлён
	- `201 Created` — ресурс создан (если ещё не существовал)
	- `400 Bad Request` — ошибка в запросе
	- `404 Not Found` — ресурс не найден
	- `500 Internal Server Error` — ошибка сервера

## Пример запроса PUT

### Обновить пользователя с ID=123
```
PUT https://api.example.com/users/123
Content-Type: application/json
```

### Тело запроса (JSON):
```json
{
  "name": "Ilia Fedin",
  "email": "ilia@example.com",
  "age": 26
}
```
### Ответ сервера:
```json
{
  "id": 123,
  "name": "Ilia Fedin",
  "email": "ilia@example.com",
  "age": 26,
  "updatedAt": "2025-09-30T12:00:00Z"
}
```

## Итог для собеса

> **PUT** — HTTP-метод для полного обновления ресурса на сервере. Не безопасный, но идемпотентный. Данные передаются в теле запроса (обычно JSON).