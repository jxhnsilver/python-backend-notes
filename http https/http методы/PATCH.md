# 🔹 HTTP метод PATCH

**PATCH** — это HTTP-метод, который используется для **частичного обновления ресурса на сервере**.

- **Не безопасный**: изменяет состояние сервера.
- **Не всегда идемпотентный**: повторный запрос может изменить ресурс, если данные отличаются или сервер обрабатывает частичные изменения особым образом.
- Данные передаются **в теле запроса (Body)**, обычно в формате JSON.

## Основные моменты

1. **Цель метода**
    - Частично изменить существующий ресурс (например, обновить только email пользователя, а не все поля).
        
2. **Передача данных**
    - В теле запроса (`Body`)
    - Формат: JSON
    - Заголовок `Content-Type` указывает формат:
		```
		Content-Type: application/json
		```
3. **Коды ответа**
	- `200 OK` — ресурс успешно обновлён
	- `204 No Content` — ресурс обновлён, тело ответа пустое
	- `400 Bad Request` — ошибка в запросе
	- `404 Not Found` — ресурс не найден
	- `500 Internal Server Error` — ошибка сервера
## Пример запроса PATCH

### Обновить email пользователя с ID=123
```
PATCH https://api.example.com/users/123
Content-Type: application/json
```
### Тело запроса (JSON):
```json
{
  "email": "newemail@example.com"
}
```
### Ответ сервера
```json
{
  "id": 123,
  "name": "Ilia Fedin",
  "email": "newemail@example.com"
}
```

## Итог для собеса

> **PATCH** — HTTP-метод для частичного обновления ресурса. Не безопасный, не всегда идемпотентный. Данные передаются в теле запроса (обычно JSON). Используется, когда нужно изменить только отдельные поля ресурса, а не весь объект.