# 🔹 HTTP метод DELETE

**DELETE** — это HTTP-метод, который используется для **удаления ресурса на сервере**.

- **Не безопасный**: изменяет состояние сервера.
- **Идемпотентный**: повторный запрос к уже удалённому ресурсу **не меняет результат**.
- Данные обычно передаются через **URL** (не в теле запроса).

---
## 📌 Основные моменты

1. **Цель метода**
    - Удалить существующий ресурс (например, пользователя, заказ, товар).
        
2. **Передача данных**
    - Через URL (`/users/123`)
    - Тело запроса обычно не используется
        
3. **Коды ответа**
    - `200 OK` — ресурс успешно удалён
    - `204 No Content` — ресурс успешно удалён, тело ответа пустое
    - `404 Not Found` — ресурс не найден
    - `401 Unauthorized` — нет авторизации
    - `500 Internal Server Error` — ошибка сервера

## Пример запроса DELETE

### Удалить пользователя с ID=123
```
DELETE https://api.example.com/users/123
```
### Пример ответа сервера
```json
{
  "message": "User deleted successfully"
}
```
или
```json
204 No Content
```

## Итог для собеса

> **DELETE** — HTTP-метод для удаления ресурса. Не безопасный, но идемпотентный. Данные передаются через URL, тело запроса обычно не используется.


