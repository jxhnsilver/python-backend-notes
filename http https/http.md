**HTTP (HyperText Transfer Protocol)** — это **протокол прикладного уровня**, используемый для передачи данных в вебе. Он лежит в основе обмена информацией между клиентом (например, браузером) и сервером.

### 🔹 Основные характеристики HTTP:

| Свойство        | Описание                                                                                               |
| --------------- | ------------------------------------------------------------------------------------------------------ |
| **Уровень OSI** | Уровень 7 — прикладной                                                                                 |
| **Транспорт**   | Работает поверх **TCP** (обычно порт **80**)                                                           |
| **Модель**      | Клиент-серверная                                                                                       |
| **Состояние**   | **Безсостоятельный** (stateless) — каждый запрос независим                                             |
| **Текстовый**   | Сообщения передаются в виде текста - Запросы и ответы передаются в **открытом виде** (без шифрования). |
### 🔹 Как это работает?

1. **Клиент** (браузер, мобильное приложение, скрипт) отправляет **HTTP-запрос** на сервер.
2. **Сервер** обрабатывает запрос и возвращает **HTTP-ответ**.
3. Клиент получает ответ и отображает/обрабатывает его.
## 🔹 2. Структура HTTP-сообщения

HTTP-сообщение бывает двух типов:
- **Запрос (Request)**
- **Ответ (Response)**
### 🔸 2.1. HTTP-запрос (Request)

Состоит из **трёх частей**:
1. **Стартовая строка (Request Line)**
2. **Заголовки (Headers)**
3. **Тело (Body, необязательное)**
```http
POST /api/users HTTP/1.1
Host: example.com
Content-Type: application/json
Content-Length: 45
Authorization: Bearer abc123

{"name": "Alice", "email": "alice@example.com"}
```
#### 🔹 Стартовая строка (Request Line)

Формат:  
`МЕТОД ПУТЬ HTTP/ВЕРСИЯ`

- **МЕТОД**: `GET`, `POST`, `PUT`, и т.д.
- **ПУТЬ**: URI без домена (например, `/api/users`)
- **ВЕРСИЯ**: `HTTP/1.1`, `HTTP/2`, и т.д.

#### 🔹 Заголовки (Headers)

Ключ-значение, разделённые двоеточием. Завершаются **пустой строкой** (`\r\n\r\n`).
Примеры важных заголовков:

- `Host` — обязательный в HTTP/1.1 (позволяет хостить несколько сайтов на одном IP)
- `Content-Type` — MIME-тип тела (`application/json`, `text/html`, `multipart/form-data`)
- `Content-Length` — размер тела в байтах
- `User-Agent` — информация о клиенте
- `Authorization` — данные для аутентификации
- `Cookie` — передача сессионных данных
#### 🔹 Тело (Body)

Содержит данные, отправляемые серверу (например, JSON, XML, данные формы).  
Используется в `POST`, `PUT`, `PATCH` и иногда в `DELETE`.

> ⚠️ В `GET` и `HEAD` тело **не должно** использоваться (технически возможно, но не рекомендуется и может быть проигнорировано).


### 🔸 2.2. HTTP-ответ (Response)

Состоит из:

1. **Стартовая строка (Status Line)**
2. **Заголовки (Headers)**
3. **Тело (Body, необязательное)**
```http
HTTP/1.1 201 Created
Content-Type: application/json
Content-Length: 63
Date: Mon, 18 Dec 2025 10:00:00 GMT

{"id": 123, "name": "Alice", "email": "alice@example.com"}
```
#### 🔹 Стартовая строка (Status Line)

Формат:  
`HTTP/ВЕРСИЯ КОД СТАТУСА ТЕКСТ`

- `201 Created` — успешное создание ресурса
- `404 Not Found`
- `500 Internal Server Error`

#### 🔹 Заголовки

Примеры:

- `Set-Cookie` — установка cookie
- `Location` — для редиректов (`3xx`)
- `Cache-Control` — управление кэшированием
- `Server` — информация о сервере
## 3. HTTP-методы (основные)
| Метод       | Безопасный? | Идемпотентный? | Использование                     |
| ----------- | ----------- | -------------- | --------------------------------- |
| **GET**     | ✅ Да        | ✅ Да           | Получение ресурса                 |
| **POST**    | ❌ Нет       | ❌ Нет          | Создание ресурса, отправка данных |
| **PUT**     | ❌ Нет       | ✅ Да           | Полная замена ресурса             |
| **PATCH**   | ❌ Нет       | ❌ (зависит)    | Частичное обновление              |
| **DELETE**  | ❌ Нет       | ✅ Да           | Удаление ресурса                  |

### 🔸 Пояснения:

- **Безопасный (Safe)** — не изменяет состояние сервера.
- **Идемпотентный (Idempotent)** — повторный вызов даёт тот же результат, что и первый.

#### Подробнее:

- **GET**:
    - Параметры передаются в URL (`?key=value`).
    - Ограничен длиной URL (~2048 символов в браузерах).
    - Кэшируется, индексируется поисковиками.
- **POST**:
    - Данные в теле.
    - Не кэшируется по умолчанию.
    - Подходит для отправки форм, файлов, создания ресурсов.
- **PUT**:
    - Отправляет **полное** представление ресурса.
    - Если ресурс не существует — создаётся по указанному URI.
    - Пример: `PUT /users/123` — обновить пользователя полностью.
- **PATCH**:
    - Отправляет **только изменения**.
    - Часто используется с JSON Patch (`application/json-patch+json`) или просто частичным JSON.
- **DELETE**:
    - Удаляет ресурс по URI.
    - Должен возвращать `204 No Content` или `200 OK`.
## 🔹 4. HTTP-статусы (группы)

| Диапазон | Значение                                                                                   |
| -------- | ------------------------------------------------------------------------------------------ |
| `1xx`    | Информационные (продолжай) — `100 Continue`                                                |
| `2xx`    | Успех — `200 OK`, `201 Created`, `204 No Content`                                          |
| `3xx`    | Перенаправления — `301 Moved Permanently`, `304 Not Modified`                              |
| `4xx`    | Ошибки клиента — `400 Bad Request`, `401 Unauthorized`, `403 Forbidden`, `404 Not Found`   |
| `5xx`    | Ошибки сервера — `500 Internal Server Error`, `502 Bad Gateway`, `503 Service Unavailable` |